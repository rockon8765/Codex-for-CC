# Backlog — 已知未完成項

> 建立於 2026-07-27。三份 FIX-PLAN 移出 skill payload 時，未完成的項目不能只留在史料裡，
> 所以彙整到這裡。**主體是「還沒做」的事**；末尾另有一張「已完成」小表，只放**曾經列在本檔
> 又被做掉**的項目，用途是防止同一題被重複開啟——完整的過程紀錄仍在 [`history/`](history/)。
> 各平台目前有哪些功能以 [README「功能差距」段](../README.md) 為準。

## 平台功能落差

| 項目 | 狀態 | 來源 |
|---|---|---|
| `codex-check.sh` 的能力面盤點與 baseline diff **尚未移植到 Linux**（macOS 549 行 vs Linux 123 行；`capability`/`baseline` 關鍵字 macOS 25/58 處、Linux 0/0） | 進行中 | [`linux-platform-notes.md`](linux-platform-notes.md) §1 |
| Linux 版 live 端到端（I8 中文簡報實跑一輪真 codex） | 未跑 | [`linux-platform-notes.md`](linux-platform-notes.md) §4.2 |
| `--disable remote_plugin` 三平台不一致（macOS 有，Windows/Linux 沒有）。這不是 Linux 落後，是 Mac 端單方面硬化（`8bbd43f`），維護者已明確**暫緩**收緊 `--disable`。要改請三平台一起改 | 暫緩 | [`linux-platform-notes.md`](linux-platform-notes.md) §1 |

## 已評估後暫緩的項目

| 項目 | 結論 | 來源 |
|---|---|---|
| `codex exec resume` 續談 | 實測行不通：resume 不吃 `--sandbox`／`-C`，`--json` 會污染 pipe。重開前必須先過 per-resume sandbox override 的 GATE | [`history/FIX-PLAN-windows-2026-07-02.md`](history/FIX-PLAN-windows-2026-07-02.md) Phase 5.3 |
| 改用 MCP／SDK 取代 `.ps1`／`.sh` 包裝 | 切換工作量高、現行腳本可運作 | [`history/FIX-PLAN-windows-2026-07-02.md`](history/FIX-PLAN-windows-2026-07-02.md) Phase 5.4 |

## 本次（2026-07-27 context-engineering 整理）產生的待辦

| 項目 | 說明 |
|---|---|
| **`-Prompt`／`-p` inline 淘汰 stage 2** | 目前是 stage 1：同時給 `-Prompt` 與 `-PromptFile` 直接報錯、單獨用 inline 出 deprecation 警告。改成硬錯誤要等一個 release window，並先確認沒有外部呼叫端還在用 |
| ~~**macOS 原生驗證（2026-07-27 批次）**~~ **已完成（2026-07-31）** | 該批已在 Mac 真機驗完，且涵蓋分支尖端 `6f7839b`——詳見下方「2026-07-27 批次的 macOS 原生驗證」列。**已 promote 進 main，驗證分支 `refactor/context-engineering-2026-07-27-pending-native-macos` 也已刪除**（本列原寫「promote 後即可刪」，那是待辦語氣，已不成立）|
| **mac／Windows 沒有 CI** | 只有 Linux 有（`.github/workflows/linux.yml`）。Windows 與 macOS 的原生回歸仍靠人工，每次 promote 都得手動跑 |
| **Windows 斷掉的 symlink：殘留不確定（守衛已加，但該分支無法實測）** | `AI-INSTALL` 1b／回滾段現在會用 `Get-Item -LiteralPath -Force` 取 directory entry 並檢查 `ReparsePoint` 屬性，**任何 link 一律中止**。已實測涵蓋：有效 junction、**斷掉的 junction**（`Get-Item -Force` 回非 null、`Attributes` 含 `ReparsePoint` → 中止）。**未涵蓋**：指向不存在目標的 **symlink**——若 `Get-Item -Force` 對它回 null，仍會落入「不存在」分支建立 `.absent`，回滾時該位置被當成全新安裝刪除。**本機無管理員權限、開發者模式亦未開啟，無法建立 NTFS symlink 實測該分支**（WSL2 的 symlink 是 Linux 語義，不能代替）。有管理員權限或開啟開發者模式的環境請補測 dangling file-link／directory-link 兩案，並據實回寫本列。注意：影響**不是零**——若斷鏈的目標日後被還原（同步軟體等），刪掉鏈等於毀掉一個會恢復的連結拓撲 |
| **macOS／Linux 偵測不到掛載點，且回滾的 `rm -rf` 會刪掉掛載內的真實資料（嚴重）** | 1b 的 link 守衛用 `find -type l`，**掛載點是目錄不是 symlink，抓不到**。完整失敗鏈：skill 樹底下有 bind mount → 1b 照常 `cp -R`（把掛載內容實體化）→ `diff -r` 仍相等 → 印出 `ts` → 之後回滾執行 `rm -rf ~/.claude/skills/超級模式` → **跨進掛載樹刪除裡面的資料**。⚠️ **這條路徑早於 link 守衛就存在**（回滾一向用 `rm -rf`，main 目前的版本也是），守衛沒有縮小它、也沒有擴大它。`find -xdev`／`rm --one-file-system` 都不夠（同檔案系統的 bind mount 仍會漏），且後者是 GNU 專屬、會弄壞共用同一段 bash 的 macOS。可靠做法是讀平台 mount table（Linux `/proc/self/mountinfo`、macOS `mount`），**寫法平台不同且本機無 root 無法建立掛載點實測**，因此未實作。已在 AI-INSTALL 用表格明確標示各平台攔得到什麼，並要求有掛載點的使用者先卸載或改用手動安裝 |
| **安裝流程的測試臺尚未接上 CI** | 2026-07-27 為驗證 1b／1c／回滾寫了測試臺（抽出文件 code block → 假 HOME 執行 → 檢查檔案系統，含變異注入），並以「對修正前版本必須 FAIL」做反向驗證。⚠️ **本列原記載「只在驗證分支上、Windows 61 案／POSIX 58 案」已不成立**——測試臺早在 [`cb6b9f7`](https://github.com/rockon8765/Codex-for-CC/commit/cb6b9f7) 就進了 repo（「依 Codex 第九輪 BLOCK」那批），2026-08-04 起為 **Windows 69 案／POSIX 68 案**。**真正還沒做的是接上 CI**：[`linux.yml`](../.github/workflows/linux.yml) 只跑 `linux/` 內的測試，**不含頂層 `tests/ai-install/`**，所以日後有人改動守衛或指紋格式，遠端不會有任何 gate 攔下——目前仍靠人工在三台機器上跑。要嘛把它接進 CI，要嘛依下一列把整個流程改寫成腳本 |
| ~~未定案：`~/.claude/settings.local.json` 會不會被載入~~ **已結案並修正（2026-07-28）** | macOS 真機實測確認它**不是** user scope 的 hook 來源，只有從家目錄啟動時才生效（那時它剛好就是專案層的 `.claude/settings.local.json`）。四組對照實測，加上該機器 143 個 session／11 個啟動目錄／14,477 次 hook 叫用中 gate **0 次**被叫用。已據此修正：兩個 POSIX snippet 的 `_comment`、`AI-INSTALL` 步驟 2 與 1b／回滾的 `setf`、三平台 `matcher-contract`（移除 local 候選**並**移除 `\|\| candidates[0]` fallback）、`run-posix.sh` 的 seed。完整結論與證據見 [`verify-settings-scope.md`](verify-settings-scope.md)；**既有使用者的診斷與修復步驟**見 [`MIGRATION-hook-settings-target.md`](MIGRATION-hook-settings-target.md)。⚠️ 原記載的「執行檔字串矛盾」是**版本方向搞反**——`legacy settings.local.json` 在 2.1.148 命中 0 次、2.1.220 命中 14 次，是**後來才加**的權限遷移碼，與 hook 來源無關；hook 來源列舉字串兩版一字不差 |
| ~~2026-07-27 批次的 macOS 原生驗證~~ **已完成（2026-07-28；尖端 2026-07-31 複驗）** | SHA `53cbc5f` 於 macOS 26.5.2 arm64 / Claude Code 2.1.163 驗證：7 筆 blob 相符、`gate-cases` **117/117**、`matcher-contract` exit 0、`consult-schema` **4/4**、`run-e2e` **11/11**（`GATE_UNDER_TEST` 確認指向 worktree）。同批的 `tests/ai-install/run-posix.sh` 在 BSD userland（bash 3.2.57）**59/59**、反向驗證 7 FAIL 與 Linux 一致——確認 GNU→可攜移植與 symlink 守衛的 fail-open 修正在 macOS 成立。交接文件：[`HANDOFF-macos-2026-07-28.md`](HANDOFF-macos-2026-07-28.md)。**2026-07-31 補驗分支尖端 `6f7839b`**——`53cbc5f` 之後又有 **8** 個 commit（本列原寫 7，2026-08-08 以 `git rev-list --count 53cbc5f..6f7839b` 實測為 8；寫 7 的話對應的區間其實是 `9922220..6f7839b`），原本核對的 7 筆 blob **有 6 筆變動**（gate hook／snippet／SKILL／`matcher-contract`／`AI-INSTALL`／`run-posix.sh`），故整批重跑：`gate-cases` **117/117**、`matcher-contract`（新版排他驗證）exit 0、`consult-schema` **4/4**、`run-e2e` **11/11**（`GATE_BLOB=f1781d6e59a06c78d43ae074545f08ea5f0740d3`）、`run-posix.sh` **64/64**；反向驗證對 `53cbc5f` 的 `AI-INSTALL` **5 FAIL**（settings-target 新斷言）、對 `06adac5` **12 FAIL**（5 + 原本 7 個 symlink）。**分支尖端在 macOS 為綠**；當時的結論是「可 promote」，**該批已於 2026-08-04 前 promote 進 main、驗證分支已刪** |
| **A2 批次的已知殘留（2026-08-09 合併前審查第五輪，維護者裁示先併、這幾項另批處理）** | 三個 P1 已在併入前修掉（`process.exit()` 截斷 stdout → 改 `process.exitCode` 自然結束；README 三段與兩份 payload orchestration 不再自己複述合併步驟、改指向 `AI-INSTALL` 步驟 2，同時封住「handler 誤寫成 `type:"prompt"` → 安裝驗證全綠但 gate 不會被叫起」；`backup-settings` 拿掉守不住的 all-or-nothing 宣稱改成 best-effort 並如實描述）。**以下三項仍開放**：**(a)** `probe-gate-registration` 的範圍說明寫「唯二例外」，實際還有第三類——**非 gate 的 handler 不驗 `type`／`command` 必備性**（`{"type":7,"command":"node /other/hook.js"}` 與 `{"type":"command"}` 都會 exit 0）。要嘛補驗、要嘛把文案改成精確列出實際驗證的欄位。**(b)** `tests/backup-settings.test.js` 的 collision 案用「預建未來 5 秒的候選檔名」觸發撞名，**不是確定性**（排程延遲或時鐘跳動會落出視窗，而且工具若因回歸而忽略撞名 exit 0，目前同樣被歸為 SKIP 而非 FAIL）。建議改用 `node --require` preload 固定 child 的 `Date`，同一手法也能精準測 hash throw／partial destination／unlink failure。**(d)** `tests/backup-settings.test.js` 的 `FIXED_MS = Date.parse("2026-08-09T03:04:05")` **沒有時區**，依規格解析為**本地時間**。macOS 驗證者指出：若剛好在該秒執行，`r.out.includes(tsOf(FIXED_MS))` 這條「注入是否生效」的自我檢查會**貌似通過**（他們實際在該時刻前 177 秒執行，已用真實時鐘值 `030044`／`030107` ≠ 釘死值 `030405` 排除，並在真實時鐘超過釘死時刻後重跑仍 9/9）。**該秒已過去，現在無法再觸發**，但常數留著就是陷阱——修法是挑一個明顯的過去時刻（或加一條「`FIXED_MS` 距 now 至少數秒」的斷言）。**(e)** `HANDOFF-macos-a2-2026-08-08.md` §3 的 `awk` 原本用雙引號包程式（`$/` 在 bash 不是合法展開所以照跑）——**已於 2026-08-09 改成單引號並實測抽出內容逐位元組相同**，此項已結案，留紀錄。 ~~**(c)** **結構性**：gate 辨識同時住在 `tools/probe-gate-registration.js` 與三份 `matcher-contract.test.js`，且兩邊規則不同~~ **已於 2026-08-09 與 A1 同批修正**（見下一列）。收斂到 `<platform>/skills/超級模式/lib/gate-registration.js`（三平台逐位元相同、隨 skill 安裝進 live），probe 與 matcher-contract 共用同一個 parser，各自有具名 verdict。**過程中發現 (c) 的描述低估了問題**：兩支工具不只規則不一致，在**欄位層面是一致地錯**——`if`／`once`／`async`／`asyncRewake` 與 `command:"echo <needle>"` 這五種情形，修正前**兩邊都印「正常／PASS」而 gate 根本不會 gate** |
| **gate 辨識共用模組 ＋ A1 批次的已知殘留（2026-08-09）** | 本批把 gate 辨識收斂成單一模組、加上 `matcher-contract` 的 `--repo`／`--live` 顯式模式，並攔下五種「有註冊但不會 gate」的設定。**以下四項刻意未做**：**(a)** **`command:"echo <needle>"` 這類 substring 假陽性仍在。** 模組一律稱它 needle **candidate**、不宣稱 registration，範圍說明也明講，但 probe 仍會判「正常」、`matcher-contract` 仍 PASS。要真的排除得在模組裡剖析 shell command 的可執行 token 與參數——那是本批唯一需要新演算法的部分，也最容易長出邊角案例，所以留著。`tests/gate-registration.test.js` 的 A51 與 `tools` 的範圍說明都釘住了現況。**(b)** **probe 沒有跨 target 診斷能力。** 在 WSL 想檢查 Windows 那邊的安裝、或在容器內想檢查掛載進來的 host home，都做不到——`os.homedir()` 只會給當前 target。合併前審查點出：這件事的主因是 **target home** 而不是模組副本，所以「掃三份鏡像」修不了它；要修得加明確的 `--home` 或 `--settings`／`--local-settings` 旗標，並整片重算 probe 現有的 61 案矩陣。**(c)** **無旗標的淘汰 stage 2（硬錯誤）未定案。** 目前 stage 1：走舊的自動判斷並印 deprecation 到 stderr。⚠️ **不要記載成「行為與退出碼完全不變」**——本批的嚴格化在無旗標路徑同樣生效，`type` 或不安全欄位壞掉時退出碼會由 0 變 1。改成硬錯誤要先確認沒有外部呼叫端還在用（與 `-Prompt`／`-p` inline 那列同一個 release window 問題）。**(d)** **`windows/settings.snippet.json` 的 `_comment` 沒有「settings 被改動後重跑 `matcher-contract --live`」那條指引**，macOS／Linux 兩份有。Windows 使用者因此少一條指引。補它只是一行，但會把「15 個既有呼叫點」變成 16 個，且要動第四份受測檔，所以與本批的 A1／(c) 範圍分開處理 |
| **`CLAUDE_CONFIG_DIR`：目前是 fail-closed，正解是單一 config-root resolver** | 官方環境變數，**覆寫整個設定目錄**（官方 `.claude` 目錄頁：設定它之後「every `~/.claude` path on this page lives under that directory instead」）。probe 與 `matcher-contract --live` 都用 `os.homedir()/.claude` 解路徑，所以變數一設就會驗到一份 Claude 不會讀的檔，還印出一條看起來像 live 的路徑 —— **假 attestation**。2026-08-09 的處置是**偵測到非空值就 fail-closed**（具名 `CONFIG_DIR_OVERRIDE`、exit 1、指出兩條出路），刻意不猜語義：本 repo 對它的邊角行為（相對路徑、尾斜線、指向不存在的目錄）沒有實測依據，猜錯只會製造出**另一個**錯誤目標。**正解**是做一個 config-root resolver，讓 probe、`--live`、以及 `AI-INSTALL` 裡所有 `~/.claude` 字面路徑共用；那會動到安裝文件的每一處路徑，範圍明顯超出當時那一批。順帶：probe 目前沒有 `--settings`／`--home` 旗標，所以設了該變數的使用者對 probe 無替代路徑（`matcher-contract` 還有 `--settings/--hook`）。⚠️ 我第一次查文件時判斷它**不存在**（WebFetch 的摘要說「not present」，第二次查又在字母序上被截斷），據此拒絕實作；合併前審查第二輪貼出官方原文才更正。**教訓：摘要式抓取不是可靠的「不存在」證據**，要嘛拿到原文、要嘛承認查不到而不是斷定沒有 |
| **極小但為正的 `timeout` 未驗證** | 2026-08-09 起 `timeout` 必須是 finite number 且 **> 0** —— 那是官方發布的 settings JSON schema 的硬門檻（`exclusiveMinimum: 0`），違反則整份 settings 被拒絕載入、gate 根本不存在。**但「多小算來不及」沒有依據**：官方沒有記載逾時後的行為，本 repo 也沒有 runtime smoke 的能力（那本身是長期已知缺口）。所以任何正值一律放行，範圍說明明文寫「不驗 timeout 的大小」。要收緊得先有真 Claude runtime 的實測，不是挑一個看起來合理的門檻 |
| **`disableAllHooks` 的偵測範圍只到家目錄那兩個檔** | 共用模組只看 caller 餵進來的檔案。**專案層 `.claude/settings.json` 或 managed 層**若設了這個總開關，probe 與 `matcher-contract` 都看不到，會回「正常」。文案已明講這個界線。要涵蓋得先決定「診斷工具要不要讀專案層」——那會牽動「從哪個目錄啟動」的整套語義，與上面那列的 config-root 是同一個 seam |
| **`matcher-contract` 的 module digest 只是線索，不是 guard** | attestation 會印共用模組的 sha256（執行時從磁碟算，不會腐爛），但**沒人比對時它不會讓任何東西失敗**。合併前審查曾要求「硬編預期 SHA 並 enforce」，第二輪自行撤回：consumer 與模組同在 payload、由 1c 一起安裝，所以「整包都舊」時硬編 SHA 完全無效（那才是主要情境），它只擋得到「1c 中途中斷造成新 consumer ＋ 舊 module」，而 1c 本身的逐檔 byte 驗證已會抓到。代價則是每次改模組都要同步一個常數、漏更新就誤紅。真正的新鮮度只能由 checkout 的 verifier（`--live`）或可信 manifest 決定 |
| **✅ 驗證資產的假綠與誤紅（2026-08-09 發現；2026-08-10 修正批次全數落地，三平台驗證完成）** | macOS 回報**產品邏輯全綠**但驗證資產有缺陷，維護者採 fix-forward（不回退產品重構）；在修好之前 README 那一段的「綠」不成立，**不得據此做 release 或安裝背書**。**必修（不可降級）**：**(1)** `probe-verdict-cases.test.js` exec form 五案釘成 `UNSUPPORTED_EXEC_FORM`（matcher 的 code），probe 真 verdict 是 `HALT_EXEC_FORM`；PASS 的原因是 `gate-registration.js` 的 `HALT_EXEC_FORM` 分支散文裡含字面 `RESULT_CODE=UNSUPPORTED_EXEC_FORM`，而測試用**未錨定** regex 取第一筆。⚠️ 注意 `/^…$/m` 配非 global `.match()` **仍取第一筆**（`m` 只改錨定語義），正解是 `/gm` 全收集 → 斷言恰一筆 → 且為最後非空行；並同時把散文裡的保留字串移除（兩道各自都要能獨立抓到）。**(2)** coverage 直方圖改統計**實測** code。**(3)** `matcher-contract-cli.test.js` 的 oracle 改精確相等：分開掃 stdout／stderr（現在無分隔串接）、跨兩 stream 合計恰一個錨定 marker、code 精確相等、code 與 exit 配對對照獨立 manifest、拒絕 spawn error／signal／null status。（實測 `"RESULT_CODE=OK_WITH_DUPLICATES".includes("RESULT_CODE=OK")` 為 true；70 案中只有 46 案釘了 marker。）**(4)** 標記唯一性不變量要涵蓋 probe **與** matcher，且含 `BAD_ARGS`／`CONFIG_DIR_OVERRIDE`／`TOOL_INTEGRITY_ERROR`／`INTERNAL_ERROR` 這些非一般 verdict 路徑；反向模式的舊版沒有 marker，要**明確豁免**而不是套現行契約。**(5)** baseline 一律用完整 SHA `5da2624e5f3f103f80ecca520f8ad272d2715ef5`，並印出 resolved commit 與 SUT／baseline blob，兩者相同就停手。**注意舊 probe 完全沒有 `RESULT_CODE`**，所以 baseline 端的 code 只能標 `N/A`，能比的是 region ＋ **exit**（目前連 exit 都沒比）。`INTENTIONAL_DIFFS` 要釘每案精確 diff signature 或 region hash（現在只驗「有差」），並檢查 orphan key。**(6)** 所有「抽舊版」指令改用 literal blob guard（probe `4ac2afb5…`、matcher `5edaa7e…`），取代「大小 ＋ `node --check`」——實測那兩道對「抽到現行版」全過。用 `mktemp -d`，不要固定 `/tmp/*.js`。**(7)** F8 證據鏈：handoff 與 README 都有「BSD 若非 `EISDIR` 會被測試抓到」的假話（`A14` 是字串注入；A-2／A-3 只驗前綴），要全部改掉並加一支直接印 `READ_DIR_CODE=<e.code>` 的原生診斷。**(8)** A-0 身分缺口：`CANON_PLAT="windows"` 讓 A-3 在任何平台都讀 Windows 的 hook／snippet，而 handoff blob 表只釘 macOS——補釘那兩個 Windows blob，或讓 A-3 明確跑受測平台的 canonical。**(9)** fixture 唯讀 fingerprint 改內容雜湊（現在只比檔案大小）；module digest 測試要核對**值**。**(10)** `oldStack` 改雙向（未宣告者意外噴 stack 也要拒）。**流程面（Codex 建議，已採納）**：抽出純函式 `parseCliOutcome(stdout, stderr, status, signal, error)` 用合成輸出釘住（散文假 code、`OK` 冒充 `OK_WITH_DUPLICATES`、零／雙 marker、跨 stream 拼接、code 對但 exit 錯…），並加 **oracle mutation**（毒化標記、只在散文注入期望 code、baseline 換成 current blob）——現有 17/17 變異只證明**模組邏輯**測試有牙齒，沒證明 CLI serializer／parser／baseline／文件 oracle 有牙齒。**✅ 2026-08-10 完成狀態**：(1)–(10) 與流程面的 `parseCliOutcome` 抽出＋oracle mutation **全部落地**（commit 7365df4／ecf89a1／7d821f3／17de399）。新增 `tests/lib/cli-outcome.js`（唯一 oracle，解析與語意比對分層）、`tests/lib/verdict-manifest.js`（手寫 code→exit 表，雙向 orphan）、`tests/oracle-teeth.test.js`（變異注入 **14/14 殺掉**，每個都驗注入自我檢查＋失敗 signature＋對照組仍綠）、`tools/diagnose-readdir-errno.js`（F8 唯一真證據）。新測試已接進 linux CI。**實測補充（backlog 原本沒記）**：marker 在 `BAD_ARGS`／`TOOL_INTEGRITY_ERROR`／`CONFIG_DIR_OVERRIDE` 三條路徑走 **stderr**、其餘走 stdout，所以「最後非空行」只能對**它所在的 stream** 成立（`spawnSync` 無法重建兩個 pipe 的全域順序）；另外 `--baseline` 只對**早於共用模組抽出**的 ref 有效，較新的 ref 抽到暫存目錄會全部退化成 `TOOL_INTEGRITY_ERROR`，已加自足性守衛。**✅ macOS 原生驗收已完成（2026-08-10）**：B-0…B-13 全綠（macOS 26.6.1 arm64／系統 bash 3.2.57／Node v26.7.0／uid=501），含 `READ_DIR_CODE=EISDIR`、`backup-settings --strict` 9/9 **SKIP 0**、matcher attestation 印 `canonical 平台：macos`。**驗收另外抓到兩個缺陷、均已修**：(a) `run-posix.sh --doc ""` 靜默退回預設文件並印 95/0 exit 0；(b) 合併前 Codex 審查再抓到 `DOC=<剛好等於預設路徑>` 搭配不同 `--doc` 時歧義漏擋。**兩者是同一個病**——拿「值」當「有沒有設過」的 sentinel；已改為明確 set 旗標並補 `run-posix-args.test.sh`（13/13，已進 Linux CI）。~~**仍未做**：Windows 側 M13（列舉失敗 fail-closed）沒有動態測試~~ **✅ 2026-08-12 已補**（單獨一批，見本表下一列）。 **可降級 backlog**：`--json`（現有「唯一 sentinel 行 ＋ exit」本來就是機器介面，缺的是 consumer 遵守契約，加 JSON 只會多一套 renderer）、真正的「任意歷史 ref」runner（歷史 topology／scope header／依賴版本都不同）、以 AST 取代 regex 掃 hook 原始碼 |
| **⚠️ Windows 側 M13：列舉失敗必須在任何 mutation 之前中止（2026-08-12 已補，單獨一批；⚠️ 2026-08-15 訂正——`[M13c]` 的覆蓋**只及 hook**，見下一列 `F2`）** | 上一列的「仍未做」。回滾的資料安全契約先前在 Windows 只有 `$ErrorActionPreference = 'Stop'` 的**靜態推論**，POSIX 側 2026-08-10 就有動態案。**注入手法＝對自己下 Deny ACE**（POSIX 是 `chmod 000`）：目錄擁有者即使沒有管理員權限也隱含保有 `WRITE_DAC`，所以拒絕自己 `ListDirectory` 再拿掉都不需要提權。新增 `[M13]` 兩變體（備份子樹／live 子樹）＋ `[M13b]`（把 `Stop` 改成 `Continue`，證明保護就是來自那一行）＋ `[M13c]`（把一個 mutation 搬到預掃之前，證明 oracle 夠寬），共 **+26 案**（86 → **112**）。**刻意不照抄 POSIX 的 root 前置守衛**：root 會忽略權限位元所以那邊必須先擋，而 Windows 的 Deny ACE 優先於 Allow、提權不會讓注入失效（`Get-ChildItem` 不用 `SeBackupPrivilege`）；真正會失效的情況（檔案系統不支援 ACL、行程啟用備份權限）無法可靠前置偵測，故改由**注入自我檢查當唯一權威**——沒生效就直接 FAIL，不給綠燈也不靜默跳過（本測試臺沒有 SKIP，加一個會改動結尾 `PASS=`／`FAIL=` 摘要行的契約）。**實測**：`pwsh` 7.6.3 與 Windows PowerShell 5.1.26100 各 **112／0 exit 0**；反向驗證對 `4a96698` 各 **101／11 exit 1**（M11 四條＋M13 四條快照＋M13c 三條），兩 host 逐條相同。與 POSIX 同樣結論：`列舉失敗 → 回滾中止` 只看退出碼、**不具區辨力**（舊版照樣 PASS）。**合併前 Codex 審查回 BLOCK，擋下一個真的 surviving mutant**：第一版 oracle 只快照 `.claude\skills`，但契約說「**任何** mutation 之前」，而產品在預掃**之後**才改 hook／settings → 把 hook mutation 搬到預掃前，第一版全綠放行。已加寬成整個假家目錄並補 `[M13c]` 當牙齒測試；同輪還修掉 `[M13b]` 改鎖備份子樹（不再耦合 `Remove-Item` 對部分不可存取樹的刪除語義）、注入自檢移進 child host、新增 `EXPECTED_CHECKS` 案數硬斷言、`Invoke-Block` 補 host 解析與 `$LASTEXITCODE` 重設／型別檢查。**過程中自我檢查先紅才發現的事（同一個坑三次）**：(1) 上一批散文含 `RESULT_CODE=`；(2) 回滾區塊的**註解**含 `$ErrorActionPreference = 'Stop'` 字面 → `String.Replace` 會連註解一起改；(3) `[M13c]` 的注入行是產品既有 `elseif (Get-Entry $hook) { … }` 的**子字串** → 直接數會得到 2 次。三次都是「未錨定／子字串比對」，修法分別是行首錨定、`Compare-Object` 驗只動一行、注入行帶唯一標記。 ⚠️ **2026-08-15 訂正**：本列上面那句「已加寬成整個假家目錄並補 `[M13c]` 當牙齒測試」**範圍講太滿**——`[M13c]` 實際只注入 **hook** 型違規，settings 型違規在 main 上**殺不掉**（原因與待辦見下一列 `F2`）。本列其餘敘述不變。 |
| **🔴 `F2`（OPEN／未實作）：Windows `[M13c]` 只涵蓋 hook，settings 型違規在 main 上殺不掉** | **穩定 ID＝`F2`；狀態＝OPEN；發現日 2026-08-14**（記憶檔第八輪稽核；起點是 Codex 反方點名 `9c24946`，實查後成立）。上一列宣稱補了 `[M13c]` 當牙齒測試，但 `main` 的 `[M13c]` **只注入 hook 型違規**，而產品在預掃**之後**同時還原 hook **與 settings**。settings 那一半測不到，**而且原因不是 oracle 不夠寬，是 fixture**：`Add-ExistingInstall` 寫的 `settings.json` 與 1b 的備份完全相同、1c 不碰 settings ⇒ 把「settings 還原」搬到預掃前只是 **OLD → OLD**，連整個假家目錄的寬 oracle 都看不到。⇒ 必須先有 `Set-Step2Settings`（模擬安裝步驟 2 把 hook 條目併進 settings）讓 settings 與備份不同，settings 型 mutant 才殺得掉。**現成修法在停擺分支上，但不可照抄 `9c24946`**：`22bb8c4..3450219` 之間只有 `9c24946` 與 `eb5cdd7` 動過 `run-windows.ps1`，`eb5cdd7` 才補上兩條防假綠（「來源本來就在 anchor 之後」、「產出與原檔確實不同」）。**正確來源＝`22bb8c4..eb5cdd7` 的淨差異，最終 blob `bfec1fafaf48e7108c81a50d2ed73fb0ff6b9227`**；`9c24946` 的 blob `e2b69652…` 已知缺牙齒。⚠️ **保存風險**：這兩個 commit 在 origin 上**只有 `test/posix-m13-wide-oracle-2026-08-12-pending-native-macos` 一條 ref 可達**（2026-08-15 窮舉 origin refs 實測），而該分支已裁示 ⛔ 停擺、不得合併——分支若被刪就沒了。**關閉條件（缺一不可）**：(a) resolution commit；(b) 兩個 Windows host 各 `126／0 exit 0`；(c) 對 `4a96698` 的固定文件 blob 反向為 `112／14 exit 1`，且失敗集合**恰為** M11 四條＋M13 四條＋M13c 六條（不可只驗「有 14 條紅」或「退出碼非零」）；(d) **至少三個具名 mutation control**——把 `Set-Step2Settings` 變 no-op、hook／settings 各自預先搬到首掃前、刪一條無副作用 `Check`；(e) 依常駐規則推 main 前諮詢。⚠️ **`EXPECTED_CHECKS` 只是最後一道結構保險，不得替代 (b)(c)(d)**：2026-08-14 實測，刪掉一個案子的 stimulus、保留 assertion，變數沿用上一次的成功結果 → **案數不變、全綠、exit 0，mutant 存活**。⚠️ 修好後也**只能**宣稱「既有安裝 copy arm 下，hook／settings 的內容型提前還原可被觀測」，**不得擴張成「完整 M13」**（`.absent` 刪除分支、純 ACL mutation、事件型副作用、被快照排除的 PowerShell AppData 子樹、Windows CI 皆仍未涵蓋）。 |
| **1b 有與 M13 同型的靜態依賴，未測（2026-08-12 Codex 指出，本批未處理）** | 1b 的 link 掃描與 `Get-TreeFingerprint` 同樣依賴區塊開頭的 `$ErrorActionPreference = 'Stop'`。若對 live skill 子樹注入 ListDirectory Deny 再把 1b 的 `Stop` 改成 `Continue`：link 掃描可能只回非終止錯誤、`Copy-Item` 可能留下**部分備份**、來源與備份的 fingerprint 可能同時看不到受限 payload，最後仍印出 `backup ts=`——而 `backup ts=` 正是「三個備份都完成」的宣稱。**這是另一條契約（備份完整性），不是回滾的資料安全契約**，所以沒有併進 M13 那一批。1c 有同型風險但契約不同（它本來就允許安裝中途留下部分更新）。另註：**預掃成功不代表後續一定可刪／可寫**（ACL 可以允許列舉卻拒絕 Delete／DeleteChild），那是既有的非交易式回滾限制，不該假裝由 M13 解決。 |
| **`run-posix.sh` 沒有案數硬斷言；POSIX 的 M13 斷言名稱也會重複** | Windows 側 2026-08-12 起有 `EXPECTED_CHECKS`（`PASS + FAIL` 不等於常數就 exit 1），POSIX 側沒有——刪掉任一案仍會印 `PASS=n FAIL=0` 並 exit 0，抓不到。另外 POSIX 的 `[M13]` 用 `[bak]`／`[live]` 標籤，與 `[M11]` 重複（例如「前置：1b 成功並印出 ts」兩處同名），逐條核對只能靠散文補前綴；Windows 側已改成斷言字串自帶 `[M13]` 前綴。兩者都是小改動，但會動到已驗收的 `run-posix.sh`，需重跑三平台，故另批。 |
| **✅ `run-windows.ps1 -Shell powershell`（5.1）一直跑不完（2026-08-12 發現並修）** | 做 M13 時順帶發現：5.1 把 native command 寫 stderr 的輸出包成 `NativeCommandError` 的 ErrorRecord，撞上檔案開頭的 `$ErrorActionPreference = 'Stop'` 就讓**整個測試臺**中止（7.x 不會）。實測連未改動的 `ad8ff12` 也一樣，停在 `[M1]` 第一個「被拒」案——那是第一個會寫 stderr 的子行程。**影響**：`tests/ai-install/README.md` 宣傳兩種跑法，但第二種從來跑不完，所以 Windows 側**實際只有 pwsh 一個 host 有覆蓋**，而 5.1 才是本 repo 記載的 hook 執行環境。修法是在 `Invoke-Block` 內以**函式作用域**降級為 `Continue`（離開函式自動還原，其餘斷言的 Stop 語義不變）；子行程成敗本來就一律以 `$LASTEXITCODE` 判斷，不該由 host 的錯誤串流決定。這是**假紅**不是假綠（會 exit 1），但代價是 5.1 的覆蓋等於零。 |
| **`run-windows.ps1` 的 112 案仍無 CI** | [`linux.yml`](../.github/workflows/linux.yml) 只跑 POSIX 側。Windows 測試臺（含新增的 M13）改壞了遠端不會有任何 gate 攔下，仍靠人工在本機跑兩個 host。與上方「安裝流程的測試臺尚未接上 CI」是同一題的 Windows 面。 |
| **🔴 POSIX 側 `[M13]` 有與 Windows 完全同型的窄 oracle（2026-08-12 第二輪 Codex 審查抓到，建議列下一批 P0）** | [`run-posix.sh`](../tests/ai-install/run-posix.sh) 的 `[M13]` 只取 `BEFORE=$(snap "$H/.claude/skills")`、最後也只比 skills，**沒有** HOME／`.claude` 的寬 oracle，也沒有 `M13c`。**可復現**：把 bash 回滾區塊裡既有的 hook mutation（`if [ -f "$hbak" ]; then cp … else rm -f …; fi`）從 skill 還原之後**原樣搬到第一個 `scan_no_link` 之前**，在非 root Linux／macOS 跑 `DOC=<mutant> bash tests/ai-install/run-posix.sh` —— `[M13][bak]`／`[M13][live]` 仍會 PASS（hook 已在預掃前被改，oracle 看不到）。Codex 以字串手術核對過：hook 區塊與首個掃描錨點都恰好命中一次、搬移後 hook 確實早於首掃。**最低修正**：POSIX 的 M13 同樣加寬到假 HOME，並補一個「exact hook-block 搬移」的牙齒測試；順便補 `EXPECTED_CHECKS` 與唯一斷言名稱（見上一列）。⚠️ **關於要不要當成 2026-08-12 那批的合併門檻**：Codex 第二輪以「CLAUDE.md 要求跨平台同步」為由 BLOCK。反面理由是該缺口**先前就在 main 上**（2026-08-10 起）、非該批引入，而修它會把 macOS 原生重驗拖進一個原本只動 `run-windows.ps1` 與文件的批次（本 repo macOS 無 CI）——與 2026-08-10 那次「Codex 兩輪想把 Windows M13 當門檻、維護者裁示不當門檻」是同一形狀的取捨。**這是維護者的裁量，待決；本列不預設結論。** |
| **✅ `tests/ai-install/run-posix.sh` 靜默忽略未知參數（2026-08-10 已修）** | 它用**環境變數** `DOC=` 指定受測文件（`DOC="${DOC:-$REPO/docs/AI-INSTALL.md}"`），而且**完全沒有參數解析**。所以 `bash run-posix.sh --doc <path>` 會被靜默忽略、改測分支自己的 `AI-INSTALL.md` → **反向驗證印出全綠卻什麼都沒量到**。2026-08-10 移植 B1 時我真的踩到：先拿到 `85 PASS／0 FAIL`（應為 81／4），是因為輸出裡有「受測文件：」那一行才發現目標沒換。Windows 版 `run-windows.ps1` 有 `param([string]$Doc=)`，PowerShell 的 param 綁定會對未知參數報錯，所以不受影響。**修法**：POSIX 版加參數解析，未知參數一律非 0 退出（與 probe／matcher-contract 現在的作法一致）。⚠️ 這與 README ⛔ 那列是**同一類缺陷**（oracle 自己不忠實），修正批次應一併處理 |
| **安裝流程改寫成 repo 內的腳本（進行中）** | 2026-07-27 的第七～九輪對抗審查連續三輪在同一份 markdown 的刪除／回滾狀態機裡找到實質缺陷（萬用字元 `ts` 繞過預檢並還原錯誤版本、內嵌 link 被實體化、POSIX collision loop 漏斷鏈、掛載點偵測不到）。**2026-07-28 維護者拍板改寫**，經兩輪對抗設計審查定案。**執行載體＝[`installer-rewrite-spec.md`](installer-rewrite-spec.md)**（含設計決策、被實測推翻的 EXDEV 假設、測試分套、shipping gate、驗收表）。要點：單一 Node 核心 + 平台 adapter；永不刪除只搬隔離區；掛載偵測涵蓋祖先與受管子樹後代；雜湊比對為動作前提；settings 走 semantic patch。**分階段交付——三平台原生綠之前不切換 `AI-INSTALL.md`，舊流程維持可用** |
| **安裝流程的殘留檔清單要跟著維護** | 1c 用「複製（合併語意）＋ 逐一刪除已知移除路徑」處理殘留，目前清單只有 `FIX-PLAN.md` 一筆。**日後若再有檔案移出 skill payload，必須同步加進 1c 的清理行**，否則舊檔會留在使用者的 live。這是刻意選的取捨：換來「安裝流程從不整個刪除 live」這個性質 |
| **安裝流程若要自動化，需重新設計為交易式** | 現行 1b/1c 是給人／AI 手動逐段執行的，沒有交易語義與併發保護（兩個同時進行的安裝會互相覆蓋）。2026-07-27 曾嘗試改成 staging＋交易目錄模型，經三輪對抗審查暴露出「刪了卻換不上」「狀態 marker 不完整」「跨交易競爭同一 live」等問題後**整批回退**——結論是交易協定不該寫在複製貼上的 markdown 裡。若日後要做排程／自動安裝，應改寫成 repo 內有測試臺的腳本，而不是繼續加厚文件 |

## 已完成（留紀錄，避免重複開題）

| 項目 | 處置 |
|---|---|
| ~~repo 缺 `.gitattributes`~~ | **2026-07-27 已加**。`* text=auto` 打底；`*.sh` 一律 `eol=lf`，另**逐檔明列** `codex-check-stubs/codex` 與 `codex-check-stubs/npm`（`linux/` 與 `macos/` 各 2 個、共 4 個無副檔名的 `#!/bin/bash` 腳本，`windows/` 沒有該目錄；`*.sh` 抓不到它們。刻意不用萬用字元，否則日後放進該目錄的 binary fixture 會被強制當文字正規化）；`*.js`／`*.json`／`*.yml`／`*.md` 也是 `eol=lf`；`*.ps1` 為 `eol=crlf`（Windows 原生執行，另有 UTF-8 BOM 需求——BOM 與行尾是兩回事）。加入後 `git status` 無偽差異，`git ls-files --eol` 確認 index 全部 `i/lf`。⚠️ **只保證新的 checkout**：既有 clone 的工作目錄不會因為加了本檔就自動重寫（詳見檔頭註解）|
| ~~`AI-INSTALL.md` 備份步驟會製造重複 skill~~ | **2026-07-27 已修**。備份改放 `~/.claude/skills-backup/`——原本放在 `~/.claude/skills/` 底下會被 skill loader 註冊成第二個 skill（description 幾乎相同、干擾選擇）。rollback 路徑一併更新，且還原改用**複製**而非搬移，備份因此留在原地、回滾可重複執行。殘留檔（`FIX-PLAN.md`）改用 1c 的逐一刪除處理，**安裝流程維持「從不整個刪除 live」** |
