# Backlog — 已知未完成項

> 建立於 2026-07-27。三份 FIX-PLAN 移出 skill payload 時，未完成的項目不能只留在史料裡，
> 所以彙整到這裡。**主體是「還沒做」的事**；末尾另有一張「已完成」小表，只放**曾經列在本檔
> 又被做掉**的項目，用途是防止同一題被重複開啟——完整的過程紀錄仍在 [`history/`](history/)。
> 各平台目前有哪些功能以 [README「功能差距」段](../README.md) 為準。
>
> ## ⛔ 凍結中（2026-08-29）—— 本表的項目**預設不得開工**
>
> 產品層已凍結，規則的唯一真相在根目錄 [`AGENTS.md`](../AGENTS.md) 的「產品凍結中」段
> （放那裡是因為 agent 會自動讀它；本段只是指路，**不複述規則**）。
> 重點：「在途」「P0」「parity」「已投入很多」「只是測試」**都不構成例外**；
> 唯一例外是**已出貨**的 security／資料遺失／上游 breakage，且須在 commit 明示 `UNFREEZE <ID>` 四行。
> **唯讀驗收不受限**（部署既有版本、跑既有套件、記錄結果），但**不得 fix-forward**。
>
> ### 狀態標籤（2026-08-29 起四種要分開，不要全叫「未做」；2026-09-14 補 `PROPOSED`／`DOC-FIX`／`UNVERIFIED` 三種）
>
> | 標籤 | 意思 | 例 |
> |---|---|---|
> | `FROZEN` | 是真的待辦，但凍結期間不做；**登錄≠承諾實作** | `WINDOWS-CI`、`QUOTA-CLASSIFIER` |
> | `REJECTED` | **不是待辦**，是已裁決的 non-goal | broker／app-server job control |
> | `ACCEPTED_RISK` | 已知、已評估、**真的裁決過**刻意接受 | credential v2、`CODEX-CHECK-WARNING` |
> | `UNSUPPORTED` | 撤回支援宣稱，不再承諾 parity | Linux live 驗收 |
> | `PROPOSED` | **提案，未裁決**：只有 spec，9/30 覆核才決定採不採 | `E1`–`E20`（上游漂移稽核） |
> | `DOC-FIX` | 文件層待辦（事實訂正或決策文件），凍結明文允許、隨時可做 | 9/6 稽核 D1／D9、`LTS-DECISION-MEMO` |
> | `UNVERIFIED` | 單方主張或只到自述層，未重現前不列為缺陷 | `BSD-GREP-INVALID-BYTES`、`WORKER-CONFIG-PLANTING` |
>
> ⚠️ 為什麼要分：先前四種全寫成「未做」，於是 `broker`（**已裁決不採**）與
> `WINDOWS-CI`（**真的該做**）在表上長得一樣，下一個 session 沒有依據判斷哪些能碰。
>
> ## 文件分工（2026-08-19 訂立，避免「每項都做了、但不是同一個 release 狀態」）
>
> - **本檔是「還沒做」的唯一真相。** 任何未完成項目都必須在這裡有一列；
>   其他文件**不得**充當未完成清單的來源。
> - `docs/*-plan-*.md`＝某一批的規畫與過程紀錄。**合併後即為史料**，
>   讀它是為了理解「當時為什麼這樣決定」，不是為了知道「現在還缺什麼」。
>   （2026-08-19 Codex 指出：plan 的「§9.2 未做」清單被當成 backlog 用，
>   造成 plan 說未完成、backlog 記舊實作、驗證散綁三個 commit 的三頭馬車。）
> - `docs/HANDOFF-*.md`＝驗證證據。**每一筆結果都要綁 commit**，
>   否則「跑過了」無法對應到「跑的是哪個 tree」。

## 平台功能落差

| 項目 | 狀態 | 來源 |
|---|---|---|
| `codex-check.sh` 的能力面盤點與 baseline diff **尚未移植到 Linux**（macOS 549 行 vs Linux 123 行；`capability`/`baseline` 關鍵字 macOS 25/58 處、Linux 0/0） | 進行中 | [`linux-platform-notes.md`](linux-platform-notes.md) §1 |
| Linux 版 live 端到端（I8 中文簡報實跑一輪真 codex） | 未跑 | [`linux-platform-notes.md`](linux-platform-notes.md) §4.2 |
| `--disable remote_plugin` 三平台不一致（macOS 有，Windows/Linux 沒有）。這不是 Linux 落後，是 Mac 端單方面硬化（`8bbd43f`），維護者已明確**暫緩**收緊 `--disable`。要改請三平台一起改。⚠️ 2026-09-14 訂正：`--disable remote_plugin` 只關遠端外掛目錄（#28443），不是硬化；註冊面有效的是 `--disable apps --disable plugins`（見 [`ACCEPTANCE-capability-boundary-2026-09-14.md`](ACCEPTANCE-capability-boundary-2026-09-14.md) 與 `CONNECTOR-EXPOSURE`） | 暫緩 | [`linux-platform-notes.md`](linux-platform-notes.md) §1 |

## 已評估後暫緩的項目

| 項目 | 結論 | 來源 |
|---|---|---|
| `codex exec resume` 續談 | 實測行不通：resume 不吃 `--sandbox`／`-C`，`--json` 會污染 pipe。重開前必須先過 per-resume sandbox override 的 GATE | [`history/FIX-PLAN-windows-2026-07-02.md`](history/FIX-PLAN-windows-2026-07-02.md) Phase 5.3 |
| 改用 MCP／SDK 取代 `.ps1`／`.sh` 包裝 | 切換工作量高、現行腳本可運作 | [`history/FIX-PLAN-windows-2026-07-02.md`](history/FIX-PLAN-windows-2026-07-02.md) Phase 5.4 |

## 本次（2026-07-27 context-engineering 整理）產生的待辦

| 項目 | 說明 |
|---|---|
| **✅ `codex-consult` 的憑證只看 exit code（2026-08-17 發現）——已修，三平台原生驗證全綠（2026-08-18）** | 判準抽成三平台共用的單一 node 模組 [`skills/超級模式/lib/consult-answer.js`](../macos/skills/超級模式/lib/consult-answer.js)（逐位元鏡像 ×3，由 `MIRRORED` 守住），三個 caller 都接上：codex `exit 0` **之後才**叫判準，不合格 → **exit 43、不鑄造、既有憑證的 bytes 與 mtime 都不動**；找不到 node／判準 → **exit 45 fail-closed**；憑證改成「同目錄暫存 → 寫入 → 讀回驗證 → 單一 rename」。**驗收（皆為實測尾行）**：`consult-answer` 42/42、`consult-answer-teeth` 27/27（8 個 mutant，比對**具名失敗集合**不比總數）、`gate-registration` 171/171、`consult-credential` Windows pwsh 7.x／WinPS 5.1 各 31/31、Linux（WSL2 ext4 clone）31/31、**原生 macOS 26.6.1 arm64 ／ `/bin/bash` 3.2.57 ／ node 26.7.0**：先在 `f601ac6` 驗 38/38、24/24、171/171、31/31，再在 `e37458c`（新增 fixture 後）複驗 42/42、27/27、31/31，五支既有回歸未變。⚠️ macOS 驗證者另跑了**對照組**：父 commit `cd2f848` 在同一台是 `29/31`（`1c`／`8d` 紅）⇒ **雙斜線修法是 load-bearing 的，31/31 不是空過**。⚠️ 三個對封存版 `380462f` 的**刻意**差異（控制字元開頭仍接受、補充平面字母計入、討論模式只驗非空），理由與 fixture 見 lib 檔頭 D1／D2／③。⚠️ **未涵蓋**（不是失敗，是沒驗）：macOS 上的 node 22／24（該機只有 26.7.0）、「caller 鑄出的 token → 真 gate 消費」的端到端接線、`os.replace` 失敗路徑注入、case-sensitive APFS。⚠️ **`case-sensitive APFS` 不只是「沒驗」**——[`super-mode-hardening-plan-2026-07.md`](super-mode-hardening-plan-2026-07.md) 早已明載：mac hook 的 `norm()` **無條件 `toLowerCase()`**（配合 APFS 出廠預設的大小寫不敏感），在**選用**的 case-sensitive 卷上 `.CLAUDE/` 可能被誤認成 `.claude/` 豁免區 ⇒ 那是**既知的支援範圍外**、且**非本批引入**，不是本批的驗證缺口。 |
| **🔴 憑證是「諮詢收據」不是「動作授權」——`BLOCK` 一樣解鎖 gate（2026-08-18 設計審查發現，未修）** | gate hook 的憑證檢查只有 `fs.existsSync(token) && Date.now() - mtimeMs < WINDOW_MS` ＋ `readCredRepo(token)` 的 repo 綁定，**完全不讀裁決欄位**。⇒ 一次形式合格的 `BLOCK:` 回覆照樣刷新 token，把原動作再解鎖 20 分鐘；`SKILL.md` §3.5 的「BLOCK 就不做」**純靠 orchestrator 自律**，工具面零強制。上面那列的 validator **不改變這件事**（它只擋「沒取得足量且合格的輸出」）。⚠️ 要真的擋，需要 credential v2 格式 ＋ hook 讀裁決 ＋ session／generation 綁定 ＋ 舊授權撤銷語義（撤銷會影響並行 session 的合法窗口，不是加個欄位就好）——**那是獨立的一批，不是上一列的延伸**。另有一個既存殘餘：**格式壞掉的新 token 會被 gate 當成「舊格式、無 repo 綁定」的收據**而只驗時間。 |
| **POSIX 側的測試臺沒有固定案數斷言（2026-08-18 macOS 驗證者指出）** | Windows 側早就有這個模式——`tests/ai-install/run-windows.ps1` 的 `EXPECTED_CHECKS`、`tests/probe-verdict-cases.test.js` 的 `EXPECTED_CASE_COUNT`——**POSIX 側沒跟上**：`run-gate-tests.js`、`run-e2e.sh`、`run-posix.sh` 被清空案例後會印 `PASS 0/0` 並 `exit 0`。⚠️ **這是既有缺口、不是憑證判準那批引進的。** ⚠️ 也要記得 repo 自己實測過的限制：**案數守衛只證明「沒少跑案」，不證明案子做了它宣稱的動作**（刪 stimulus、留 assertion 的 mutant 案數不變仍全綠），所以補案數斷言是**下限**不是牙齒。 |
| **`-Prompt`／`-p` inline 淘汰 stage 2** | 目前是 stage 1：同時給 `-Prompt` 與 `-PromptFile` 直接報錯、單獨用 inline 出 deprecation 警告。改成硬錯誤要等一個 release window，並先確認沒有外部呼叫端還在用 |
| ~~**macOS 原生驗證（2026-07-27 批次）**~~ **已完成（2026-07-31）** | 該批已在 Mac 真機驗完，且涵蓋分支尖端 `6f7839b`——詳見下方「2026-07-27 批次的 macOS 原生驗證」列。**已 promote 進 main，驗證分支 `refactor/context-engineering-2026-07-27-pending-native-macos` 也已刪除**（本列原寫「promote 後即可刪」，那是待辦語氣，已不成立）|
| **mac／Windows 沒有 CI** | 只有 Linux 有（`.github/workflows/linux.yml`）。Windows 與 macOS 的原生回歸仍靠人工，每次 promote 都得手動跑 |
| **Windows 斷掉的 symlink：殘留不確定（守衛已加，但該分支無法實測）** | `AI-INSTALL` 1b／回滾段現在會用 `Get-Item -LiteralPath -Force` 取 directory entry 並檢查 `ReparsePoint` 屬性，**任何 link 一律中止**。已實測涵蓋：有效 junction、**斷掉的 junction**（`Get-Item -Force` 回非 null、`Attributes` 含 `ReparsePoint` → 中止）。**未涵蓋**：指向不存在目標的 **symlink**——若 `Get-Item -Force` 對它回 null，仍會落入「不存在」分支建立 `.absent`，回滾時該位置被當成全新安裝刪除。**本機無管理員權限、開發者模式亦未開啟，無法建立 NTFS symlink 實測該分支**（WSL2 的 symlink 是 Linux 語義，不能代替）。有管理員權限或開啟開發者模式的環境請補測 dangling file-link／directory-link 兩案，並據實回寫本列。注意：影響**不是零**——若斷鏈的目標日後被還原（同步軟體等），刪掉鏈等於毀掉一個會恢復的連結拓撲 |
| **macOS／Linux 偵測不到掛載點，且回滾的 `rm -rf` 會刪掉掛載內的真實資料（嚴重）** | 1b 的 link 守衛用 `find -type l`，**掛載點是目錄不是 symlink，抓不到**。完整失敗鏈：skill 樹底下有 bind mount → 1b 照常 `cp -R`（把掛載內容實體化）→ `diff -r` 仍相等 → 印出 `ts` → 之後回滾執行 `rm -rf ~/.claude/skills/超級模式` → **跨進掛載樹刪除裡面的資料**。⚠️ **這條路徑早於 link 守衛就存在**（回滾一向用 `rm -rf`，main 目前的版本也是），守衛沒有縮小它、也沒有擴大它。`find -xdev`／`rm --one-file-system` 都不夠（同檔案系統的 bind mount 仍會漏），且後者是 GNU 專屬、會弄壞共用同一段 bash 的 macOS。可靠做法是讀平台 mount table（Linux `/proc/self/mountinfo`、macOS `mount`），**寫法平台不同且本機無 root 無法建立掛載點實測**，因此未實作。已在 AI-INSTALL 用表格明確標示各平台攔得到什麼，並要求有掛載點的使用者先卸載或改用手動安裝 |
| **安裝流程的測試臺尚未接上 CI** | 2026-07-27 為驗證 1b／1c／回滾寫了測試臺（抽出文件 code block → 假 HOME 執行 → 檢查檔案系統，含變異注入），並以「對修正前版本必須 FAIL」做反向驗證。⚠️ **本列原記載「只在驗證分支上、Windows 61 案／POSIX 58 案」已不成立**——測試臺早在 [`cb6b9f7`](https://github.com/rockon8765/Codex-for-CC/commit/cb6b9f7) 就進了 repo（「依 Codex 第九輪 BLOCK」那批），2026-08-04 起為 **Windows 69 案／POSIX 68 案**。**真正還沒做的是接上 CI**：[`linux.yml`](../.github/workflows/linux.yml) 只跑 `linux/` 內的測試，**不含頂層 `tests/ai-install/`**，所以日後有人改動守衛或指紋格式，遠端不會有任何 gate 攔下——目前仍靠人工在三台機器上跑。要嘛把它接進 CI，要嘛依 [`INSTALLER-REWRITE`](#INSTALLER-REWRITE) 把整個流程改寫成腳本（⚠️ 原文寫「依**下一列**」是錯的——下一列是 `settings.local.json` 那條已結案項，真正的目標在本表後段，2026-08-17 改成 ID 互指）|
| ~~未定案：`~/.claude/settings.local.json` 會不會被載入~~ **已結案並修正（2026-07-28）** | macOS 真機實測確認它**不是** user scope 的 hook 來源，只有從家目錄啟動時才生效（那時它剛好就是專案層的 `.claude/settings.local.json`）。四組對照實測，加上該機器 143 個 session／11 個啟動目錄／14,477 次 hook 叫用中 gate **0 次**被叫用。已據此修正：兩個 POSIX snippet 的 `_comment`、`AI-INSTALL` 步驟 2 與 1b／回滾的 `setf`、三平台 `matcher-contract`（移除 local 候選**並**移除 `\|\| candidates[0]` fallback）、`run-posix.sh` 的 seed。完整結論與證據見 [`verify-settings-scope.md`](verify-settings-scope.md)；**既有使用者的診斷與修復步驟**見 [`MIGRATION-hook-settings-target.md`](MIGRATION-hook-settings-target.md)。⚠️ 原記載的「執行檔字串矛盾」是**版本方向搞反**——`legacy settings.local.json` 在 2.1.148 命中 0 次、2.1.220 命中 14 次，是**後來才加**的權限遷移碼，與 hook 來源無關；hook 來源列舉字串兩版一字不差 |
| ~~2026-07-27 批次的 macOS 原生驗證~~ **已完成（2026-07-28；尖端 2026-07-31 複驗）** | SHA `53cbc5f` 於 macOS 26.5.2 arm64 / Claude Code 2.1.163 驗證：7 筆 blob 相符、`gate-cases` **117/117**、`matcher-contract` exit 0、`consult-schema` **4/4**、`run-e2e` **11/11**（`GATE_UNDER_TEST` 確認指向 worktree）。同批的 `tests/ai-install/run-posix.sh` 在 BSD userland（bash 3.2.57）**59/59**、反向驗證 7 FAIL 與 Linux 一致——確認 GNU→可攜移植與 symlink 守衛的 fail-open 修正在 macOS 成立。交接文件：[`HANDOFF-macos-2026-07-28.md`](HANDOFF-macos-2026-07-28.md)。**2026-07-31 補驗分支尖端 `6f7839b`**——`53cbc5f` 之後又有 **8** 個 commit（本列原寫 7，2026-08-08 以 `git rev-list --count 53cbc5f..6f7839b` 實測為 8；寫 7 的話對應的區間其實是 `9922220..6f7839b`），原本核對的 7 筆 blob **有 6 筆變動**（gate hook／snippet／SKILL／`matcher-contract`／`AI-INSTALL`／`run-posix.sh`），故整批重跑：`gate-cases` **117/117**、`matcher-contract`（新版排他驗證）exit 0、`consult-schema` **4/4**、`run-e2e` **11/11**（`GATE_BLOB=f1781d6e59a06c78d43ae074545f08ea5f0740d3`）、`run-posix.sh` **64/64**；反向驗證對 `53cbc5f` 的 `AI-INSTALL` **5 FAIL**（settings-target 新斷言）、對 `06adac5` **12 FAIL**（5 + 原本 7 個 symlink）。**分支尖端在 macOS 為綠**；當時的結論是「可 promote」，**該批已於 2026-08-04 前 promote 進 main、驗證分支已刪** |
| **A2 批次的已知殘留（2026-08-09 合併前審查第五輪，維護者裁示先併、這幾項另批處理）** | 三個 P1 已在併入前修掉（`process.exit()` 截斷 stdout → 改 `process.exitCode` 自然結束；README 三段與兩份 payload orchestration 不再自己複述合併步驟、改指向 `AI-INSTALL` 步驟 2，同時封住「handler 誤寫成 `type:"prompt"` → 安裝驗證全綠但 gate 不會被叫起」；`backup-settings` 拿掉守不住的 all-or-nothing 宣稱改成 best-effort 並如實描述）。**以下三項仍開放**：**(a)** `probe-gate-registration` 的範圍說明寫「唯二例外」，實際還有第三類——**非 gate 的 handler 不驗 `type`／`command` 必備性**（`{"type":7,"command":"node /other/hook.js"}` 與 `{"type":"command"}` 都會 exit 0）。要嘛補驗、要嘛把文案改成精確列出實際驗證的欄位。**(b)** `tests/backup-settings.test.js` 的 collision 案用「預建未來 5 秒的候選檔名」觸發撞名，**不是確定性**（排程延遲或時鐘跳動會落出視窗，而且工具若因回歸而忽略撞名 exit 0，目前同樣被歸為 SKIP 而非 FAIL）。建議改用 `node --require` preload 固定 child 的 `Date`，同一手法也能精準測 hash throw／partial destination／unlink failure。**(d)** `tests/backup-settings.test.js` 的 `FIXED_MS = Date.parse("2026-08-09T03:04:05")` **沒有時區**，依規格解析為**本地時間**。macOS 驗證者指出：若剛好在該秒執行，`r.out.includes(tsOf(FIXED_MS))` 這條「注入是否生效」的自我檢查會**貌似通過**（他們實際在該時刻前 177 秒執行，已用真實時鐘值 `030044`／`030107` ≠ 釘死值 `030405` 排除，並在真實時鐘超過釘死時刻後重跑仍 9/9）。**該秒已過去，現在無法再觸發**，但常數留著就是陷阱——修法是挑一個明顯的過去時刻（或加一條「`FIXED_MS` 距 now 至少數秒」的斷言）。**(e)** `HANDOFF-macos-a2-2026-08-08.md` §3 的 `awk` 原本用雙引號包程式（`$/` 在 bash 不是合法展開所以照跑）——**已於 2026-08-09 改成單引號並實測抽出內容逐位元組相同**，此項已結案，留紀錄。 ~~**(c)** **結構性**：gate 辨識同時住在 `tools/probe-gate-registration.js` 與三份 `matcher-contract.test.js`，且兩邊規則不同~~ **已於 2026-08-09 與 A1 同批修正**（見下一列）。收斂到 `<platform>/skills/超級模式/lib/gate-registration.js`（三平台逐位元相同、隨 skill 安裝進 live），probe 與 matcher-contract 共用同一個 parser，各自有具名 verdict。**過程中發現 (c) 的描述低估了問題**：兩支工具不只規則不一致，在**欄位層面是一致地錯**——`if`／`once`／`async`／`asyncRewake` 與 `command:"echo <needle>"` 這五種情形，修正前**兩邊都印「正常／PASS」而 gate 根本不會 gate** |
| **gate 辨識共用模組 ＋ A1 批次的已知殘留（2026-08-09）** | 本批把 gate 辨識收斂成單一模組、加上 `matcher-contract` 的 `--repo`／`--live` 顯式模式，並攔下五種「有註冊但不會 gate」的設定。**以下四項刻意未做**：**(a)** **`command:"echo <needle>"` 這類 substring 假陽性仍在。** 模組一律稱它 needle **candidate**、不宣稱 registration，範圍說明也明講，但 probe 仍會判「正常」、`matcher-contract` 仍 PASS。要真的排除得在模組裡剖析 shell command 的可執行 token 與參數——那是本批唯一需要新演算法的部分，也最容易長出邊角案例，所以留著。`tests/gate-registration.test.js` 的 A51 與 `tools` 的範圍說明都釘住了現況。**(b)** **probe 沒有跨 target 診斷能力。** 在 WSL 想檢查 Windows 那邊的安裝、或在容器內想檢查掛載進來的 host home，都做不到——`os.homedir()` 只會給當前 target。合併前審查點出：這件事的主因是 **target home** 而不是模組副本，所以「掃三份鏡像」修不了它；要修得加明確的 `--home` 或 `--settings`／`--local-settings` 旗標，並整片重算 probe 現有的 61 案矩陣。**(c)** **無旗標的淘汰 stage 2（硬錯誤）未定案。** 目前 stage 1：走舊的自動判斷並印 deprecation 到 stderr。⚠️ **不要記載成「行為與退出碼完全不變」**——本批的嚴格化在無旗標路徑同樣生效，`type` 或不安全欄位壞掉時退出碼會由 0 變 1。改成硬錯誤要先確認沒有外部呼叫端還在用（與 `-Prompt`／`-p` inline 那列同一個 release window 問題）。**(d)** **`windows/settings.snippet.json` 的 `_comment` 沒有「settings 被改動後重跑 `matcher-contract --live`」那條指引**，macOS／Linux 兩份有。Windows 使用者因此少一條指引。補它只是一行，但會把「15 個既有呼叫點」變成 16 個，且要動第四份受測檔，所以與本批的 A1／(c) 範圍分開處理 |
| **`CLAUDE_CONFIG_DIR`：目前是 fail-closed，正解是單一 config-root resolver** | 官方環境變數，**覆寫整個設定目錄**（官方 `.claude` 目錄頁：設定它之後「every `~/.claude` path on this page lives under that directory instead」）。probe 與 `matcher-contract --live` 都用 `os.homedir()/.claude` 解路徑，所以變數一設就會驗到一份 Claude 不會讀的檔，還印出一條看起來像 live 的路徑 —— **假 attestation**。2026-08-09 的處置是**偵測到非空值就 fail-closed**（具名 `CONFIG_DIR_OVERRIDE`、exit 1、指出兩條出路），刻意不猜語義：本 repo 對它的邊角行為（相對路徑、尾斜線、指向不存在的目錄）沒有實測依據，猜錯只會製造出**另一個**錯誤目標。**正解**是做一個 config-root resolver，讓 probe、`--live`、以及 `AI-INSTALL` 裡所有 `~/.claude` 字面路徑共用；那會動到安裝文件的每一處路徑，範圍明顯超出當時那一批。順帶：probe 目前沒有 `--settings`／`--home` 旗標，所以設了該變數的使用者對 probe 無替代路徑（`matcher-contract` 還有 `--settings/--hook`）。⚠️ 我第一次查文件時判斷它**不存在**（WebFetch 的摘要說「not present」，第二次查又在字母序上被截斷），據此拒絕實作；合併前審查第二輪貼出官方原文才更正。**教訓：摘要式抓取不是可靠的「不存在」證據**，要嘛拿到原文、要嘛承認查不到而不是斷定沒有 |
| **極小但為正的 `timeout` 未驗證** | 2026-08-09 起 `timeout` 必須是 finite number 且 **> 0** —— 那是官方發布的 settings JSON schema 的硬門檻（`exclusiveMinimum: 0`），違反則整份 settings 被拒絕載入、gate 根本不存在。**但「多小算來不及」沒有依據**：官方沒有記載逾時後的行為，本 repo 也沒有 runtime smoke 的能力（那本身是長期已知缺口）。所以任何正值一律放行，範圍說明明文寫「不驗 timeout 的大小」。要收緊得先有真 Claude runtime 的實測，不是挑一個看起來合理的門檻 |
| **`disableAllHooks` 的偵測範圍只到家目錄那兩個檔** | 共用模組只看 caller 餵進來的檔案。**專案層 `.claude/settings.json` 或 managed 層**若設了這個總開關，probe 與 `matcher-contract` 都看不到，會回「正常」。文案已明講這個界線。要涵蓋得先決定「診斷工具要不要讀專案層」——那會牽動「從哪個目錄啟動」的整套語義，與上面那列的 config-root 是同一個 seam |
| **`matcher-contract` 的 module digest 只是線索，不是 guard** | attestation 會印共用模組的 sha256（執行時從磁碟算，不會腐爛），但**沒人比對時它不會讓任何東西失敗**。合併前審查曾要求「硬編預期 SHA 並 enforce」，第二輪自行撤回：consumer 與模組同在 payload、由 1c 一起安裝，所以「整包都舊」時硬編 SHA 完全無效（那才是主要情境），它只擋得到「1c 中途中斷造成新 consumer ＋ 舊 module」，而 1c 本身的逐檔 byte 驗證已會抓到。代價則是每次改模組都要同步一個常數、漏更新就誤紅。真正的新鮮度只能由 checkout 的 verifier（`--live`）或可信 manifest 決定 |
| **✅ 驗證資產的假綠與誤紅（2026-08-09 發現；2026-08-10 修正批次全數落地，三平台驗證完成）** | macOS 回報**產品邏輯全綠**但驗證資產有缺陷，維護者採 fix-forward（不回退產品重構）；在修好之前 README 那一段的「綠」不成立，**不得據此做 release 或安裝背書**。**必修（不可降級）**：**(1)** `probe-verdict-cases.test.js` exec form 五案釘成 `UNSUPPORTED_EXEC_FORM`（matcher 的 code），probe 真 verdict 是 `HALT_EXEC_FORM`；PASS 的原因是 `gate-registration.js` 的 `HALT_EXEC_FORM` 分支散文裡含字面 `RESULT_CODE=UNSUPPORTED_EXEC_FORM`，而測試用**未錨定** regex 取第一筆。⚠️ 注意 `/^…$/m` 配非 global `.match()` **仍取第一筆**（`m` 只改錨定語義），正解是 `/gm` 全收集 → 斷言恰一筆 → 且為最後非空行；並同時把散文裡的保留字串移除（兩道各自都要能獨立抓到）。**(2)** coverage 直方圖改統計**實測** code。**(3)** `matcher-contract-cli.test.js` 的 oracle 改精確相等：分開掃 stdout／stderr（現在無分隔串接）、跨兩 stream 合計恰一個錨定 marker、code 精確相等、code 與 exit 配對對照獨立 manifest、拒絕 spawn error／signal／null status。（實測 `"RESULT_CODE=OK_WITH_DUPLICATES".includes("RESULT_CODE=OK")` 為 true；70 案中只有 46 案釘了 marker。）**(4)** 標記唯一性不變量要涵蓋 probe **與** matcher，且含 `BAD_ARGS`／`CONFIG_DIR_OVERRIDE`／`TOOL_INTEGRITY_ERROR`／`INTERNAL_ERROR` 這些非一般 verdict 路徑；反向模式的舊版沒有 marker，要**明確豁免**而不是套現行契約。**(5)** baseline 一律用完整 SHA `5da2624e5f3f103f80ecca520f8ad272d2715ef5`，並印出 resolved commit 與 SUT／baseline blob，兩者相同就停手。**注意舊 probe 完全沒有 `RESULT_CODE`**，所以 baseline 端的 code 只能標 `N/A`，能比的是 region ＋ **exit**（目前連 exit 都沒比）。`INTENTIONAL_DIFFS` 要釘每案精確 diff signature 或 region hash（現在只驗「有差」），並檢查 orphan key。**(6)** 所有「抽舊版」指令改用 literal blob guard（probe `4ac2afb5…`、matcher `5edaa7e…`），取代「大小 ＋ `node --check`」——實測那兩道對「抽到現行版」全過。用 `mktemp -d`，不要固定 `/tmp/*.js`。**(7)** F8 證據鏈：handoff 與 README 都有「BSD 若非 `EISDIR` 會被測試抓到」的假話（`A14` 是字串注入；A-2／A-3 只驗前綴），要全部改掉並加一支直接印 `READ_DIR_CODE=<e.code>` 的原生診斷。**(8)** A-0 身分缺口：`CANON_PLAT="windows"` 讓 A-3 在任何平台都讀 Windows 的 hook／snippet，而 handoff blob 表只釘 macOS——補釘那兩個 Windows blob，或讓 A-3 明確跑受測平台的 canonical。**(9)** fixture 唯讀 fingerprint 改內容雜湊（現在只比檔案大小）；module digest 測試要核對**值**。**(10)** `oldStack` 改雙向（未宣告者意外噴 stack 也要拒）。**流程面（Codex 建議，已採納）**：抽出純函式 `parseCliOutcome(stdout, stderr, status, signal, error)` 用合成輸出釘住（散文假 code、`OK` 冒充 `OK_WITH_DUPLICATES`、零／雙 marker、跨 stream 拼接、code 對但 exit 錯…），並加 **oracle mutation**（毒化標記、只在散文注入期望 code、baseline 換成 current blob）——現有 17/17 變異只證明**模組邏輯**測試有牙齒，沒證明 CLI serializer／parser／baseline／文件 oracle 有牙齒。**✅ 2026-08-10 完成狀態**：(1)–(10) 與流程面的 `parseCliOutcome` 抽出＋oracle mutation **全部落地**（commit 7365df4／ecf89a1／7d821f3／17de399）。新增 `tests/lib/cli-outcome.js`（唯一 oracle，解析與語意比對分層）、`tests/lib/verdict-manifest.js`（手寫 code→exit 表，雙向 orphan）、`tests/oracle-teeth.test.js`（變異注入 **14/14 殺掉**，每個都驗注入自我檢查＋失敗 signature＋對照組仍綠）、`tools/diagnose-readdir-errno.js`（F8 唯一真證據）。新測試已接進 linux CI。**實測補充（backlog 原本沒記）**：marker 在 `BAD_ARGS`／`TOOL_INTEGRITY_ERROR`／`CONFIG_DIR_OVERRIDE` 三條路徑走 **stderr**、其餘走 stdout，所以「最後非空行」只能對**它所在的 stream** 成立（`spawnSync` 無法重建兩個 pipe 的全域順序）；另外 `--baseline` 只對**早於共用模組抽出**的 ref 有效，較新的 ref 抽到暫存目錄會全部退化成 `TOOL_INTEGRITY_ERROR`，已加自足性守衛。**✅ macOS 原生驗收已完成（2026-08-10）**：B-0…B-13 全綠（macOS 26.6.1 arm64／系統 bash 3.2.57／Node v26.7.0／uid=501），含 `READ_DIR_CODE=EISDIR`、`backup-settings --strict` 9/9 **SKIP 0**、matcher attestation 印 `canonical 平台：macos`。**驗收另外抓到兩個缺陷、均已修**：(a) `run-posix.sh --doc ""` 靜默退回預設文件並印 95/0 exit 0；(b) 合併前 Codex 審查再抓到 `DOC=<剛好等於預設路徑>` 搭配不同 `--doc` 時歧義漏擋。**兩者是同一個病**——拿「值」當「有沒有設過」的 sentinel；已改為明確 set 旗標並補 `run-posix-args.test.sh`（13/13，已進 Linux CI）。~~**仍未做**：Windows 側 M13（列舉失敗 fail-closed）沒有動態測試~~ **✅ 2026-08-12 已補**（單獨一批，見本表下一列）。 **可降級 backlog**：`--json`（現有「唯一 sentinel 行 ＋ exit」本來就是機器介面，缺的是 consumer 遵守契約，加 JSON 只會多一套 renderer）、真正的「任意歷史 ref」runner（歷史 topology／scope header／依賴版本都不同）、以 AST 取代 regex 掃 hook 原始碼 |
| **⚠️ Windows 側 M13：列舉失敗必須在任何 mutation 之前中止（2026-08-12 已補，單獨一批；⚠️ 2026-08-15 訂正——`[M13c]` 的覆蓋**只及 hook**，見下一列 `F2`）** | 上一列的「仍未做」。回滾的資料安全契約先前在 Windows 只有 `$ErrorActionPreference = 'Stop'` 的**靜態推論**，POSIX 側 2026-08-10 就有動態案。**注入手法＝對自己下 Deny ACE**（POSIX 是 `chmod 000`）：目錄擁有者即使沒有管理員權限也隱含保有 `WRITE_DAC`，所以拒絕自己 `ListDirectory` 再拿掉都不需要提權。新增 `[M13]` 兩變體（備份子樹／live 子樹）＋ `[M13b]`（把 `Stop` 改成 `Continue`，證明保護就是來自那一行）＋ `[M13c]`（把一個 mutation 搬到預掃之前，證明 oracle 夠寬），共 **+26 案**（86 → **112**）。**刻意不照抄 POSIX 的 root 前置守衛**：root 會忽略權限位元所以那邊必須先擋，而 Windows 的 Deny ACE 優先於 Allow、提權不會讓注入失效（`Get-ChildItem` 不用 `SeBackupPrivilege`）；真正會失效的情況（檔案系統不支援 ACL、行程啟用備份權限）無法可靠前置偵測，故改由**注入自我檢查當唯一權威**——沒生效就直接 FAIL，不給綠燈也不靜默跳過（本測試臺沒有 SKIP，加一個會改動結尾 `PASS=`／`FAIL=` 摘要行的契約）。**實測**：`pwsh` 7.6.3 與 Windows PowerShell 5.1.26100 各 **112／0 exit 0**；反向驗證對 `4a96698` 各 **101／11 exit 1**（M11 四條＋M13 四條快照＋M13c 三條），兩 host 逐條相同。與 POSIX 同樣結論：`列舉失敗 → 回滾中止` 只看退出碼、**不具區辨力**（舊版照樣 PASS）。**合併前 Codex 審查回 BLOCK，擋下一個真的 surviving mutant**：第一版 oracle 只快照 `.claude\skills`，但契約說「**任何** mutation 之前」，而產品在預掃**之後**才改 hook／settings → 把 hook mutation 搬到預掃前，第一版全綠放行。已加寬成整個假家目錄並補 `[M13c]` 當牙齒測試；同輪還修掉 `[M13b]` 改鎖備份子樹（不再耦合 `Remove-Item` 對部分不可存取樹的刪除語義）、注入自檢移進 child host、新增 `EXPECTED_CHECKS` 案數硬斷言、`Invoke-Block` 補 host 解析與 `$LASTEXITCODE` 重設／型別檢查。**過程中自我檢查先紅才發現的事（同一個坑三次）**：(1) 上一批散文含 `RESULT_CODE=`；(2) 回滾區塊的**註解**含 `$ErrorActionPreference = 'Stop'` 字面 → `String.Replace` 會連註解一起改；(3) `[M13c]` 的注入行是產品既有 `elseif (Get-Entry $hook) { … }` 的**子字串** → 直接數會得到 2 次。三次都是「未錨定／子字串比對」，修法分別是行首錨定、`Compare-Object` 驗只動一行、注入行帶唯一標記。 ⚠️ **2026-08-15 訂正**：本列上面那句「已加寬成整個假家目錄並補 `[M13c]` 當牙齒測試」**範圍講太滿**——`[M13c]` 實際只注入 **hook** 型違規，settings 型違規在 main 上**殺不掉**（原因與待辦見下一列 `F2`）。本列其餘敘述不變。 |
| **✅ `F2`（已修，2026-08-17）：Windows `[M13c]` 只涵蓋 hook，settings 型違規在 main 上殺不掉** | **穩定 ID＝`F2`；狀態＝已修**（本列的描述講的是**修好之前**的 `main@22bb8c4`，保留原文以免日後誤讀為現況；關閉證據見本列末尾）。**發現日 2026-08-14**（記憶檔第八輪稽核；起點是 Codex 反方點名 `9c24946`，實查後成立）。上一列宣稱補了 `[M13c]` 當牙齒測試，但 `main` 的 `[M13c]` **只注入 hook 型違規**，而產品在預掃**之後**同時還原 hook **與 settings**。settings 那一半測不到，**而且原因不是 oracle 不夠寬，是 fixture**：`Add-ExistingInstall` 寫的 `settings.json` 與 1b 的備份完全相同、1c 不碰 settings ⇒ 把「settings 還原」搬到預掃前只是 **OLD → OLD**，連整個假家目錄的寬 oracle 都看不到。⇒ 必須先有 `Set-Step2Settings`（模擬安裝步驟 2 把 hook 條目併進 settings）讓 settings 與備份不同，settings 型 mutant 才殺得掉。**現成修法在停擺分支上，但不可照抄 `9c24946`**：`22bb8c4..3450219` 之間只有 `9c24946` 與 `eb5cdd7` 動過 `run-windows.ps1`，`eb5cdd7` 才補上兩條防假綠（「來源本來就在 anchor 之後」、「產出與原檔確實不同」）。**正確來源＝`22bb8c4..eb5cdd7` 的淨差異，最終 blob `bfec1fafaf48e7108c81a50d2ed73fb0ff6b9227`**；`9c24946` 的 blob `e2b69652…` 已知缺牙齒。⚠️ ~~**保存風險**：這兩個 commit 在 origin 上**只有 `test/posix-m13-wide-oracle-2026-08-12-pending-native-macos` 一條 ref 可達**（2026-08-15 窮舉 origin refs 實測），而該分支已裁示 ⛔ 停擺、不得合併——分支若被刪就沒了。~~ **已解除（2026-08-17）**：該分支確實已刪，但刪除前先打了 annotated tag [`archive/posix-m13-wide-oracle-2026-08-13`](https://github.com/rockon8765/Codex-for-CC/tree/archive/posix-m13-wide-oracle-2026-08-13)（→ `3450219`），刪除後已實測 `git show <tag>^{commit}:<path>` 仍抓得到內容，`eb5cdd7`／`9c24946` 兩個 commit 也仍可解析。**關閉條件（缺一不可）**：(a) resolution commit；(b) 兩個 Windows host 各 `126／0 exit 0`；(c) 對 `4a96698` 的固定文件 blob 反向為 `112／14 exit 1`，且失敗集合**恰為** M11 四條＋M13 四條＋M13c 六條（不可只驗「有 14 條紅」或「退出碼非零」）；(d) **至少三個具名 mutation control**——把 `Set-Step2Settings` 變 no-op、hook／settings 各自預先搬到首掃前、刪一條無副作用 `Check`；(e) 依常駐規則推 main 前諮詢。⚠️ **`EXPECTED_CHECKS` 只是最後一道結構保險，不得替代 (b)(c)(d)**：2026-08-14 實測，刪掉一個案子的 stimulus、保留 assertion，變數沿用上一次的成功結果 → **案數不變、全綠、exit 0，mutant 存活**。⚠️ 修好後也**只能**宣稱「既有安裝 copy arm 下，hook／settings 的內容型提前還原可被觀測」，**不得擴張成「完整 M13」**（`.absent` 刪除分支、純 ACL mutation、事件型副作用、被快照排除的 PowerShell AppData 子樹、Windows CI 皆仍未涵蓋）。 **✅ 關閉紀錄（2026-08-17）**：(a) resolution commit ＝ 本批；(b) 兩 host 各 `PASS=126 FAIL=0` rc=0（`pwsh` 7.6.3／WinPS 5.1.26100.9168），**本輪自行重跑復現**，不是沿用 08-15 的數字；(c) 對 `4a96698` 反向各 `PASS=112 FAIL=14` rc=1，失敗集合恰為 `[M11]` 4＋`[M13]` 4＋`[M13c]` 6；(d) **四個**具名 mutation control 全數執行，**兩 host 各 4/4 符合預期**，且已收進 repo 成為可重跑的 [`tests/ai-install/run-mutation-controls.ps1`](../tests/ai-install/run-mutation-controls.ps1)（預期數字釘死，對不上就 exit 1）——`Set-Step2Settings` 改 no-op、hook 預移、**settings 預移**、刪一條無副作用 `Check`。⚠️ **settings 預移正是 08-15 驗收缺的那一個**（(d) 明文要求 hook／settings 各自做，當時只做了 hook），2026-08-17 補跑，兩 host 各 `PASS=122 FAIL=4` rc=1；(e) 推 main 前依常駐規則諮詢 Codex。逐案證據與 log sha256 見 [`ACCEPTANCE-windows-m13c-settings-2026-08-15.md`](ACCEPTANCE-windows-m13c-settings-2026-08-15.md)。**宣稱範圍不變**——上面那句「只能宣稱什麼」仍然完全適用，本列的關閉**不**代表 M13 完整。 |
| **1b 有與 M13 同型的靜態依賴，未測（2026-08-12 Codex 指出，本批未處理）** | 1b 的 link 掃描與 `Get-TreeFingerprint` 同樣依賴區塊開頭的 `$ErrorActionPreference = 'Stop'`。若對 live skill 子樹注入 ListDirectory Deny 再把 1b 的 `Stop` 改成 `Continue`：link 掃描可能只回非終止錯誤、`Copy-Item` 可能留下**部分備份**、來源與備份的 fingerprint 可能同時看不到受限 payload，最後仍印出 `backup ts=`——而 `backup ts=` 正是「三個備份都完成」的宣稱。**這是另一條契約（備份完整性），不是回滾的資料安全契約**，所以沒有併進 M13 那一批。1c 有同型風險但契約不同（它本來就允許安裝中途留下部分更新）。另註：**預掃成功不代表後續一定可刪／可寫**（ACL 可以允許列舉卻拒絕 Delete／DeleteChild），那是既有的非交易式回滾限制，不該假裝由 M13 解決。 |
| <a id="POSIX-RUNNER-ASSERTIONS"></a>**`run-posix.sh` 沒有案數硬斷言；POSIX 的 M13 斷言名稱也會重複**（ID：`POSIX-RUNNER-ASSERTIONS`）| Windows 側 2026-08-12 起有 `EXPECTED_CHECKS`（`PASS + FAIL` 不等於常數就 exit 1），POSIX 側沒有——刪掉任一案仍會印 `PASS=n FAIL=0` 並 exit 0，抓不到。另外 POSIX 的 `[M13]` 用 `[bak]`／`[live]` 標籤，與 `[M11]` 重複（例如「前置：1b 成功並印出 ts」兩處同名），逐條核對只能靠散文補前綴；Windows 側已改成斷言字串自帶 `[M13]` 前綴。兩者都是小改動，但會動到已驗收的 `run-posix.sh`，需重跑三平台，故另批。 |
| **✅ `run-windows.ps1 -Shell powershell`（5.1）一直跑不完（2026-08-12 發現並修）** | 做 M13 時順帶發現：5.1 把 native command 寫 stderr 的輸出包成 `NativeCommandError` 的 ErrorRecord，撞上檔案開頭的 `$ErrorActionPreference = 'Stop'` 就讓**整個測試臺**中止（7.x 不會）。實測連未改動的 `ad8ff12` 也一樣，停在 `[M1]` 第一個「被拒」案——那是第一個會寫 stderr 的子行程。**影響**：`tests/ai-install/README.md` 宣傳兩種跑法，但第二種從來跑不完，所以 Windows 側**實際只有 pwsh 一個 host 有覆蓋**，而 5.1 才是本 repo 記載的 hook 執行環境。修法是在 `Invoke-Block` 內以**函式作用域**降級為 `Continue`（離開函式自動還原，其餘斷言的 Stop 語義不變）；子行程成敗本來就一律以 `$LASTEXITCODE` 判斷，不該由 host 的錯誤串流決定。這是**假紅**不是假綠（會 exit 1），但代價是 5.1 的覆蓋等於零。 |
| **`run-windows.ps1` 仍無 CI** | [`linux.yml`](../.github/workflows/linux.yml) 只跑 POSIX 側。Windows 測試臺（含新增的 M13）改壞了遠端不會有任何 gate 攔下，仍靠人工在本機跑兩個 host。與上方「安裝流程的測試臺尚未接上 CI」是同一題的 Windows 面。⚠️ **這個數字會隨每次加案變動**，唯一權威是 `run-windows.ps1` 的 `$EXPECTED_CHECKS`（本列 112→126 就是 `F2` 那批改的，改了案數卻忘了同步本列）；引用時請直接看那個常數，不要抄這裡。 |
| <a id="POSIX-M13-GAP"></a>**POSIX 側 `[M13]` 有與 Windows 完全同型的窄 oracle**（ID：`POSIX-M13-GAP`；2026-08-12 第二輪 Codex 審查抓到，**當時 Codex 建議列下一批 P0——那是 Codex 的建議，不是維護者裁決**）⚠️ **2026-08-17 狀態：缺口本身仍在 `main` 上；優先級待裁；本列當時開的候選處方已證明不足，不得直接套用**（見 [`POSIX-M13-SNAP-ABORTED`](#POSIX-M13-SNAP-ABORTED)）| [`run-posix.sh`](../tests/ai-install/run-posix.sh) 的 `[M13]` 只取 `BEFORE=$(snap "$H/.claude/skills")`、最後也只比 skills，**沒有** HOME／`.claude` 的寬 oracle，也沒有 `M13c`。**可復現**：把 bash 回滾區塊裡既有的 hook mutation（`if [ -f "$hbak" ]; then cp … else rm -f …; fi`）從 skill 還原之後**原樣搬到第一個 `scan_no_link` 之前**，在非 root Linux／macOS 跑 `DOC=<mutant> bash tests/ai-install/run-posix.sh` —— `[M13][bak]`／`[M13][live]` 仍會 PASS（hook 已在預掃前被改，oracle 看不到）。Codex 以字串手術核對過：hook 區塊與首個掃描錨點都恰好命中一次、搬移後 hook 確實早於首掃。~~**最低修正**~~ **當時的候選處方（2026-08-17 標註：已證明不足，不得直接套用）**：POSIX 的 M13 同樣加寬到假 HOME，並補一個「exact hook-block 搬移」的牙齒測試；順便補 `EXPECTED_CHECKS` 與唯一斷言名稱（見 [`POSIX-RUNNER-ASSERTIONS`](#POSIX-RUNNER-ASSERTIONS)——**原文寫「見上一列」是錯的**，位置指涉在插入新列後不可靠，2026-08-17 改成 ID 互指）。⚠️ **關於要不要當成 2026-08-12 那批的合併門檻**：Codex 第二輪以「CLAUDE.md 要求跨平台同步」為由 BLOCK。反面理由是該缺口**先前就在 main 上**（2026-08-10 起）、非該批引入，而修它會把 macOS 原生重驗拖進一個原本只動 `run-windows.ps1` 與文件的批次（本 repo macOS 無 CI）——與 2026-08-10 那次「Codex 兩輪想把 Windows M13 當門檻、維護者裁示不當門檻」是同一形狀的取捨。**這是維護者的裁量，待決；本列不預設結論。** |
| <a id="POSIX-M13-SNAP-ABORTED"></a>**⛔ [`POSIX-M13-GAP`](#POSIX-M13-GAP) 的舊實作線已停擺（2026-08-13），不得接續其尖端**（ID：`POSIX-M13-SNAP-ABORTED`）| 2026-08-12 曾依 [`POSIX-M13-GAP`](#POSIX-M13-GAP) 開了 POSIX 批次（分支 `test/posix-m13-wide-oracle-2026-08-12-pending-native-macos`，已刪），**2026-08-13 由維護者裁示停擺**。裁示理由：**連續三輪修正每輪都引進新的假綠**——方向性哨兵在雙向 oracle 下必然有一邊假綠、`unlock_tree` 在觀測前正規化權限＝主動銷毀 mode 證據、`find` 的不明非零被 `UNREADABLE-DIR` marker 吞掉（從輸出反推失敗原因）、`ls -ld` 接 `cut` 再次遮蔽左側退出碼（與同次編輯剛修的 `cksum` 同型、隔兩行又犯）——當時判定為「在用補丁追自己製造的複雜度」，並裁示 `snap` **另批重新設計**。⚠️ **這條裁示的範圍是「不要接續舊尖端」，不是「這個缺口不再需要做」**——[`POSIX-M13-GAP`](#POSIX-M13-GAP) 的優先級仍未裁決。⚠️ 也**不要過度推廣**：`3450219` 證明的是**那條 `snap` 補丁線不收斂**，並不足以證明「寬 HOME 觀測」或「mutation control」這些手法本身不該用。重新設計時要先面對一個更根本的限制：**snapshot 只能證明最終狀態相同，不能證明「中途從未 mutation」**；若契約真的是時間順序，需要事件型 instrumentation，或把狀態機放進未來的 Node installer／platform adapter 測試裡。舊尖端保存在 tag [`archive/posix-m13-wide-oracle-2026-08-13`](https://github.com/rockon8765/Codex-for-CC/tree/archive/posix-m13-wide-oracle-2026-08-13)（`3450219`）：`run-posix.sh` 700 行 vs 當時 main 413 行，**相對其基底實際是淨增 287 行，不是「丟掉 700 行」**——別把它當 sunk cost 的錨點。**僅供參考、不得合併、不得接續。** |
| **✅ `tests/ai-install/run-posix.sh` 靜默忽略未知參數（2026-08-10 已修）** | 它用**環境變數** `DOC=` 指定受測文件（`DOC="${DOC:-$REPO/docs/AI-INSTALL.md}"`），而且**完全沒有參數解析**。所以 `bash run-posix.sh --doc <path>` 會被靜默忽略、改測分支自己的 `AI-INSTALL.md` → **反向驗證印出全綠卻什麼都沒量到**。2026-08-10 移植 B1 時我真的踩到：先拿到 `85 PASS／0 FAIL`（應為 81／4），是因為輸出裡有「受測文件：」那一行才發現目標沒換。Windows 版 `run-windows.ps1` 有 `param([string]$Doc=)`，PowerShell 的 param 綁定會對未知參數報錯，所以不受影響。**修法**：POSIX 版加參數解析，未知參數一律非 0 退出（與 probe／matcher-contract 現在的作法一致）。⚠️ 這與 README ⛔ 那列是**同一類缺陷**（oracle 自己不忠實），修正批次應一併處理 |
| <a id="INSTALLER-REWRITE"></a>**安裝流程改寫成 repo 內的腳本（進行中）**（ID：`INSTALLER-REWRITE`）| 2026-07-27 的第七～九輪對抗審查連續三輪在同一份 markdown 的刪除／回滾狀態機裡找到實質缺陷（萬用字元 `ts` 繞過預檢並還原錯誤版本、內嵌 link 被實體化、POSIX collision loop 漏斷鏈、掛載點偵測不到）。**2026-07-28 維護者拍板改寫**，經兩輪對抗設計審查定案。**執行載體＝[`installer-rewrite-spec.md`](installer-rewrite-spec.md)**（含設計決策、被實測推翻的 EXDEV 假設、測試分套、shipping gate、驗收表）。要點：單一 Node 核心 + 平台 adapter；永不刪除只搬隔離區；掛載偵測涵蓋祖先與受管子樹後代；雜湊比對為動作前提；settings 走 semantic patch。**分階段交付——三平台原生綠之前不切換 `AI-INSTALL.md`，舊流程維持可用** |
| **安裝流程的殘留檔清單要跟著維護** | 1c 用「複製（合併語意）＋ 逐一刪除已知移除路徑」處理殘留，目前清單只有 `FIX-PLAN.md` 一筆。**日後若再有檔案移出 skill payload，必須同步加進 1c 的清理行**，否則舊檔會留在使用者的 live。這是刻意選的取捨：換來「安裝流程從不整個刪除 live」這個性質 |
| **安裝流程若要自動化，需重新設計為交易式** | 現行 1b/1c 是給人／AI 手動逐段執行的，沒有交易語義與併發保護（兩個同時進行的安裝會互相覆蓋）。2026-07-27 曾嘗試改成 staging＋交易目錄模型，經三輪對抗審查暴露出「刪了卻換不上」「狀態 marker 不完整」「跨交易競爭同一 live」等問題後**整批回退**——結論是交易協定不該寫在複製貼上的 markdown 裡。若日後要做排程／自動安裝，應改寫成 repo 內有測試臺的腳本，而不是繼續加厚文件 |

| <a id="EXITCODE-CONTRACT"></a>**✅ 退出碼契約（2026-08-19 修完；規畫與過程見 [`exit-code-contract-plan-2026-08-19.md`](exit-code-contract-plan-2026-08-19.md)）**（ID：`EXITCODE-CONTRACT`）| 契約＝「哨兵訊息 ＋ 專屬退出碼」。曾有**四條**獨立的路會讓它塌成 rc 1，全部已修：<br>**(1)** `Write-Warning` 緊接 `exit`（`$WarningPreference='Stop'` 下變終止性例外）→ 改 `[Console]::Error.WriteLine`。<br>**(2)** native 非零退出（`$ErrorActionPreference='Stop'` **且** `$PSNativeCommandUseErrorActionPreference=$true`）→ capture scope 內同時降兩個 preference 並還原；這是 POSIX 版 `set +e … set -e` 的平台翻譯。<br>**(3)** log 寫入失敗中止 pipeline（POSIX 是 `{ …; } >> "$log"` 在 `set -e` 下）→ 完整 drain、只記第一個錯、延遲裁決。<br>**(4)** `$var` 緊接多位元組字元被 bash 3.2 併進變數名（`set -u` → unbound → 中止）→ 一律 `${var}`，並加 [`tests/no-multibyte-varref.test.js`](../tests/no-multibyte-varref.test.js) 靜態守衛。<br>**新語義**：`exit 46` = 逐字稿不可用／寫壞（不得回報成功、不得鑄證）；消費契約改成 tuple「rc 42 **且** 精確哨兵」（三份 SKILL.md ＋ 三份 CLAUDE-global-rule.md 已同步）。<br>**驗證**（每筆綁 commit，詳見 [`HANDOFF-macos-exit-code-contract-2026-08-19.md`](HANDOFF-macos-exit-code-contract-2026-08-19.md)）：Windows `run-windows-suite` repo 9/9、live ran=8 skipped=1 fail=0；原生 macOS bash 3.2.57 ＋ BSD userland；**原生 Linux（GitHub ubuntu-latest, bash 5.2.21）smoke 22/0 @ `8eb4e08`**，且已永久接進 [`linux.yml`](../.github/workflows/linux.yml)。⚠️ **狀態訂正（2026-08-28）：原本這裡列的三條，`TRANSCRIPT-PREFLIGHT` 與 `POSIX-EXEC-QUIET` 已於同日以程式碼複驗後改標 ✅（見該兩列），仍未做的只剩 `QUOTA-CLASSIFIER` 一條。** 另新增 [`MAC-LIVE-GATE-DRIFT`](#MAC-LIVE-GATE-DRIFT)。以下為原文：⚠️ **仍未做的殘留另列下方三條**（`QUOTA-CLASSIFIER`／`TRANSCRIPT-PREFLIGHT`／`POSIX-EXEC-QUIET`），不要因為本列是 ✅ 就以為整條契約面已無缺口。 |
| <a id="QUOTA-CLASSIFIER"></a>**🟡 `FROZEN` 配額分類器：已從「整段找子字串」改成兩層判準，但精度仍有已知缺口（2026-08-19）**（ID：`QUOTA-CLASSIFIER`；**真待辦，凍結期間不做**）| **已做**：判準來源改成記憶體捕捉的 stderr（不再重讀 log ⇒ 分類正確性不再綁在磁碟寫入成功上）；判準改成 Tier 1「stderr **尾端 40 行**內、**行首錯誤標記**的行 ∧ 命中配額字樣」才給 42，Tier 2「有字樣但不在錯誤行上」只附提示、不下判斷；三平台判準對齊（`ERROR` 後接非英數或行尾、數字兩側非英數）；哨兵文案降級為「疑似…（未確證）」。依據是 88 份真實逐字稿的實證：**47 份只在 stderr 命中且幾乎全是成功諮詢**（codex 把推理軌跡與工具輸出寫在 stderr，內含 grep 行號前綴與本 repo 原始碼裡的 `QUOTA` 字串）。<br>⚠️ **仍未修的已知缺口**：(a) `ERROR: MCP quota-monitor failed to initialize` 這種「含 quota 字樣但與配額無關」的錯誤行仍會誤判成配額；(b) 真配額 ERROR 之後若有超過 40 行清理訊息就**漏判**。<br>⚠️ **為什麼不繼續調**：**手上沒有任何一份「真的配額耗盡」的逐字稿樣本**，調任何閾值都是猜，只會製造下一個沒有證據支撐的 heuristic。**取得真樣本是解除這條的前提。**<br>⚠️ Codex 的提醒（已接受）：把文案降級成「疑似」**只改善誠實度，沒有改善 recall 或 precision** ——消費端仍把「哨兵＋42」當權威控制訊號，假陽性仍會停止重試、假陰性仍完全沒有訊號。**不得**把文案降級當成本條的驗收理由。 |
| <a id="TRANSCRIPT-PREFLIGHT"></a>**✅ log 目錄建立與 temp brief 寫入已納入 exit 46 契約（2026-08-19 修完；2026-08-28 訂正狀態）**（ID：`TRANSCRIPT-PREFLIGHT`）| **⚠️ 狀態訂正（2026-08-28）**：本列原標 🔴 未修，與同表 [`EXITCODE-CONTRACT`](#EXITCODE-CONTRACT) 宣稱「四條全部已修」互相矛盾——而 backlog 自稱是「還沒做什麼」的唯一真相，這種矛盾比單純過時更糟。**以程式碼為準複驗**：`windows/skills/超級模式/scripts/codex-consult.ps1` 的逐字稿目錄（`:233`）、log 建檔（`:246`）、暫存簡報寫入（`:263`）三處都已在 `CONSULT_TRANSCRIPT_UNAVAILABLE` ＋ exit 46 契約之內 ⇒ 改標 ✅。以下保留修好之前的原始描述，勿誤讀為現況。<br>**（原始描述）** `codex-consult.ps1` 的 logdir `New-Item` 與 temp brief 寫入位於 transcript 的 catch 之外：在 `$ErrorActionPreference='Stop'` ＋ 唯讀父目錄／無效 TEMP／滿碟時會直接 rc 1，**沒有 46 哨兵**。這**屬於退出碼契約本身**，不是另一個無關的失敗面（2026-08-19 Codex 指出，我接受這個歸類）。 |
| <a id="POSIX-EXEC-QUIET"></a>**✅ POSIX `codex-exec.sh` 的 `-q` 分支 transport 已修（2026-08-19；2026-08-28 訂正狀態）**（ID：`POSIX-EXEC-QUIET`）| **⚠️ 狀態訂正（2026-08-28）**：同上，本列原標 🔴 與 [`EXITCODE-CONTRACT`](#EXITCODE-CONTRACT) 矛盾。**以程式碼為準複驗**：`macos/linux` 的 `codex-exec.sh` quiet 分支已改成與非 quiet 分支同一條 pipeline ＋ `PIPESTATUS`（`set +e` → `pipe_rc=("${PIPESTATUS[@]}")` → `log_tee_rc` → `transcript_error`），原始碼註解亦記載該修正 ⇒ 改標 ✅。以下保留修好之前的原始描述，勿誤讀為現況。<br>**（原始描述）** quiet 分支仍在啟動前直接 `>> "$log"`：log 開檔／寫入失敗時分不出 transport failure 與 codex 的退出碼，也不保證完整 drain。同屬 transport／退出碼契約與三平台等價性。 |
| <a id="MAC-LIVE-GATE-DRIFT"></a>**✅ macOS live gate 陳舊 —— 已修，P0-16 macOS 側五項全綠（2026-08-29 關閉）**（ID：`MAC-LIVE-GATE-DRIFT`；狀態 `CLOSED`）| **✅ 關閉紀錄（2026-08-29）**：需要修**兩個**既有 live 漂移才達成，**兩者都與本批無關**（`git diff 325065c..tip -- '*hooks*'` 完全空白）。**根因 A**＝live hook 停在 `b103184`（2026-07-16）⇒ `run-gate-tests` `PASS 105/117`（**12 紅**：Monitor 6／Artifact 2／ScheduleWakeup 1／Worktree 2／A2 子代理 1）；部署 tip 版 hook 後轉 `117/117`。**根因 B**＝live `settings.json` 的 matcher 自 `09340e3`（2026-07-26）起就缺 `Monitor`／`Artifact`／`ScheduleWakeup`／`EnterWorktree`／`ExitWorktree` 五個名字 ⇒ hook 修好後 `matcher-contract --live` **仍**紅（轉 `MATCHER_DRIFT`）；就地替換 matcher 字串（**非 append**，`PreToolUse entries` 前後皆 3、gate 條目皆 1）後轉 `RESULT_CODE=OK`。⚠️ **根因 B 被根因 A 遮蔽**（`matcher-contract` 在 `HOOK_UNPARSEABLE` 時會在契約比對**之前**返回），是 no-deploy 反事實 `--settings <live> --hook <repo>` 讓它提早浮出。**最終 live 五項全綠**：`117/117`、`RESULT_CODE=OK`、`11 passed 0 failed`、`27/0`、`18/0`；exact-tip＝20 個 payload 檔相符 ＋ 兩個具名 `.bak-20260713` 排除項。逐項證據見 [`ACCEPTANCE-p0-16-live-macos-2026-08-29.md`](ACCEPTANCE-p0-16-live-macos-2026-08-29.md)。**衍生修正**：根因 B 暴露 `AI-INSTALL` 步驟 2 的缺口——判定矩陣 route 在 `probe-gate-registration.js` 上，而**該 probe 不驗 matcher 內容**（本例改前改後都回 `OK_NORMAL`），已於 `13cd098` 補上範圍警語。**過程教訓（值得保留）**：第二輪曾把這 12 紅回報成「2 紅」，是「Windows 反事實預測 12、macOS 回報 2」的**數字對不上**才逼出重查——**歸因看起來成立但數字對不上時，先別放行**。<br>**（以下為關閉前的原始描述，勿誤讀為現況）** **受驗 tuple**：tip `63e601a`／原生 macOS（Darwin 25.6.0 arm64、`/bin/bash` 3.2.57、brew bash 5.3.15、BSD grep 2.6.0、node 26.7.0）／2026-08-28。**live digest**：skill 樹＝分支 tip（20 個 payload 檔逐位元相符；另有兩個 **2026-07-13 的 `.bak-*` 排除項**，部署前即存在、非本輪產生）；hook＝`b103184`（2026-07-16）的 blob `0d8667de4156a944`；settings 未取 digest。**觀察到**：AI-INSTALL 步驟 3 的 macOS live 五項為 **3 綠 2 紅** —— 綠＝`run-e2e.sh` 11/0（`GATE_UNDER_TEST` 指向 live hook）、live `exit-code-contract.smoke` 27/0、live `fault-injection.smoke` 18/0；紅＝`run-gate-tests.js`、`matcher-contract --live`（`RESULT_CODE=HOOK_UNPARSEABLE`）。**歸因**：陳舊 hook 是**充分原因**（已證：本批 `git diff 325065c..tip -- '*hooks*'` **完全空白**；`b103184` 兩平台皆 `Monitor=0`、`若你是子代理=0`，而 tip 皆 `8`／`1`；Windows 上以同組測試單換 hook 版本重現 —— tip hook `109/109` vs `b103184` **`97/109`**，含 Mac 回報的同名失敗 `T5 Monitor: 命令替換 denies`）。⚠️ **但尚未證明是唯一原因**：`matcher-contract` 在 `HOOK_UNPARSEABLE` 時會在做 matcher 契約比對**之前**就返回，所以 live `settings.json` 若也缺 `Monitor` 會被遮住。⚠️ **另有未解落差**：Windows 反事實顯示舊 hook 應造成 **12** 個 case 失敗（三平台 gate-cases 的內建工具面案數相同：Monitor 8／Artifact 3／ScheduleWakeup 2／Worktree 2），但 Mac 回報 `run-gate-tests` 只有 **2** 紅 —— 可能是回報摘要不完整，也可能另有成因，**未釐清前不得宣稱歸因完備**。**release 處置**：待使用者裁決；若獲一次性豁免則標 `WAIVED@<release>`，⚠️ **waiver ≠ closed**。**關閉條件**：同一個受驗 tuple 下 macOS live 五項**全綠**，且上述兩個未解點（唯一原因、12 vs 2）各有具名證據。**不需要**部署 hook 就能推進的下一步：兩個 no-deploy 反事實（`matcher-contract --settings <live> --hook <repo>`；把 live skill 樹鏡像到暫存目錄後改配 repo hook 跑 `run-gate-tests`，預期 117/117）。<br>**✅ 2026-08-28 收斂（原生 macOS，tip `021a399`）**：上述兩個未解點**都已解決**。<br>（1）**12 vs 2 的落差＝上輪漏報**，不是另有成因。實測 `grep -c '^FAIL'` ＝ **12**（`PASS 105/117`），與 Windows 反事實的 12 逐條吻合（Monitor 6／Artifact 2／ScheduleWakeup 1／Worktree 2／A2 子代理分支 1）。反事實 (b)「live runner/cases × repo hook」＝ **`PASS 117/117`** ⇒ 那 12 紅**全部**來自 hook 版本，與 live 的 runner/cases 無關。<br>（2）**第二根因確認存在**：反事實 (a) 回 **`MATCHER_DRIFT`**，且**在部署前就已浮出**（該實驗用的就是 repo hook）。live `settings.json` 的 matcher 缺 5 個工具名——`Monitor`／`Artifact`／`ScheduleWakeup`／`EnterWorktree`／`ExitWorktree`。溯源：這 5 個名字是 [`09340e3`](https://github.com/rockon8765/Codex-for-CC/commit/09340e3)（**2026-07-26**）同時加進 hook 與 `macos/settings.snippet.json` 的 ⇒ **該台 macOS 的 settings 自 2026-07-26 起就落後，與本批無關**。對照：Windows live settings 五個名字**全有**，僅 macOS 漂移。<br>**部署 hook 後的 live 五項**：4 綠 1 紅——`run-gate-tests` **117/117**、`run-e2e` 11/0、`exit-code-contract` 27/0、`fault-injection` 18/0；紅的只剩 `matcher-contract --live`＝`MATCHER_DRIFT`。exact-tip @ `021a399`：**20 個 payload 檔相符 ＋ 兩個具名 `.bak-20260713` 排除項**。<br>⚠️ **剩下的唯一關閉條件＝更新該台 macOS 的 `~/.claude/settings.json` matcher**（AI-INSTALL 步驟 2）。在那之前，**hook 雖已是最新版，但那 5 個工具在 runtime 下根本不會叫起 hook ⇒ 部署 hook 對它們是 no-op**。settings 屬安全關鍵檔，須使用者明確同意才動；Mac 端已依指示停手未改（`settings.json` mtime 停在 Aug 22 10:18，與備份逐位元組相同）。 |
| <a id="CODEX-CHECK-WARNING"></a>**🟡 `ACCEPTED_RISK` `codex-check.ps1` 有同型缺陷（2026-08-19 發現，刻意未做）**（ID：`CODEX-CHECK-WARNING`；**已評估後刻意接受，不是待辦**）| 與 [`EXITCODE-CONTRACT`](#EXITCODE-CONTRACT) 同機制，後果不只退出碼：<br>`:268` 緊接 `UPDATE_BASELINE=REFUSED` 與 `exit 2` → Stop 之下**哨兵與 rc 一起消失**；`:289`／`:293` 緊接 `return` → 整支中止，首次安裝還沒有 baseline 時 smoke 根本不跑；`:468` `& cmd.exe` 之後才抓 `$smokeExit` → 擷取前中止；`:513`／`:515` 緊接 `Remove-Item` 清快取 → **壞快取不會被刪**，下次非 `-Force` 仍命中舊綠快取，把壞掉的 codex 報成可用。<br>⚠️ 延後理由（先前寫錯過一次，已訂正）：**不是**「改了會炸 test runner」（`codex-check.tests.ps1:277-281` 早就把 EAP 降成 `Continue`）。**真正的理由**是該 runner 把 SUT 的 host 寫死成 `powershell.exe`，要驗這批修正得先讓它能選 host。現況：`codex-check.tests.ps1` **188/0**（2026-08-19 pwsh 7 實測），維持原狀。 |
| <a id="BSD-GREP-INVALID-BYTES"></a>**⬜ `UNVERIFIED` BSD grep 對無效位元組的行為（未重現，Codex 單方主張）**（ID：`BSD-GREP-INVALID-BYTES`；**未重現前不列為缺陷**）| Codex 表示它測到「stderr 含無效位元組時，BSD grep 在 `C` 下命中、在 `en_US.UTF-8` 下不命中」，屬獨立於 multibyte var-ref 的分類器風險。**我沒有重現**，也沒有把它當成阻擋。要處理的話第一步是取得可重現的樣本。 |
| <a id="WINDOWS-CI"></a>**🔴 `FROZEN` Windows 側仍無 CI（只有 Linux 有；macOS 也沒有——2026-09-14 訂正，原文「POSIX 已有」讀起來像 macOS 也有）**（ID：`WINDOWS-CI`；⚠️ **這是「重啟產品開發」的前置條件** —— 見 [`AGENTS.md`](../AGENTS.md) 凍結段。⚠️ 補它不只是加 YAML：現有 suite 的 `codex-check` SUT 寫死 `powershell.exe`，會漏掉 pwsh／EAP 差異，需要 dual-host gate）| [`linux.yml`](../.github/workflows/linux.yml) 現在涵蓋 POSIX 的 gate、e2e、`run-posix-args`、退出碼契約 smoke 與 multibyte 靜態守衛；**Windows 側**（`run-windows-suite.ps1`、`tests/ai-install/run-windows.ps1`）仍完全靠人工在本機跑兩個 host。改壞了遠端不會有任何 gate 攔下。 |

## 上游漂移稽核登錄（2026-09-14；來源：9/6 與 9/14 兩份稽核）

> 依 9/14 稽核 §8 的 Codex 反方意見：本節只做**登錄、去重、標證據狀態**，不代表採納。`FROZEN`＝真待辦但凍結不做；`PROPOSED`＝提案待 9/30 裁決；`DOC-FIX`＝凍結允許、隨時可做；證據狀態沿用稽核（**CONFIRMED**＝主線親讀碼／跑指令；**CANDIDATE**＝agent 引用一手文件、未重現；**UNVERIFIED**＝只到自述或單方主張）。來源：[`AUDIT-upstream-drift-2026-09-06.md`](AUDIT-upstream-drift-2026-09-06.md)（下稱 9/6）、[`AUDIT-upstream-drift-2026-09-14.md`](AUDIT-upstream-drift-2026-09-14.md)（下稱 9/14）。**已在 9/14 做掉的不列**（桶 2 八項事實訂正、`codex-check` baseline 重設）。9/6 B6（`approvals_reviewer=auto_review` 會取代 `never`）目前惰性、無待辦，僅記於此。

### A. 缺陷／曝露（產品碼，凍結中）

| 項目 | 說明 |
|---|---|
| <a id="CONNECTOR-EXPOSURE"></a>**🔴 `FROZEN` worker／consult 繼承帳號已連結的 connector 與 MCP（UNFREEZE 候選，本輪裁決**不解凍**）**（ID：`CONNECTOR-EXPOSURE`；9/6 B1、9/14 N5／§5；驗收 [`ACCEPTANCE-capability-boundary-2026-09-14.md`](ACCEPTANCE-capability-boundary-2026-09-14.md)） | 分層（2026-09-14 驗收後）：installed／enabled **CONFIRMED**；`REGISTERED` **CONFIRMED**（read-only consult 啟動遙測 `mcp_servers="cua_repl, codex_apps, node_repl"`）；`CATALOG_PRESENT` **CONFIRMED**（同分鐘刷新的 `codex_apps` 目錄 205 工具，含 Gmail 寄信／刪信、Drive 上傳／刪除／分享、Calendar 建刪、Notion 建頁、Plugin Management、Safety Settings；寄信／建事件／上傳的 annotation 是**非破壞**）；`AUTHENTICATED_CALLABLE`／`SENSITIVE_READ`／`EXTERNAL_WRITE`／核准行為 **UNVERIFIED**（刻意未探測）。控制手段：`--disable apps --disable plugins` 在**本次遙測**移除 `codex_apps` 與 `cua_repl`（原始碼 `ext/mcp/src/lib.rs:37` 支持 apps 半邊）；單旗標未對照、exec 依賴（bundled skills／文件外掛）未盤點、其他外部存取途徑未證隔離；`-c mcp_servers.cua_repl.enabled=false` 會造成 `invalid transport` 設定錯誤（plugin MCP 不能這樣單獨關）。**解凍要件**（Codex 9/14）：指出已出貨 `main` 的哪個受支援配置、什麼觸發、突破哪條預期限制、接觸哪個受保護資產；認證唯讀呼叫成功**不自動**構成例外。下一步（使用者決定）：隔離測試帳號＋合成資料做一次唯讀 connector 呼叫；真帳號需明確授權。 |
| <a id="EXEC-NO-SUBAGENTS"></a>**🔴 `FROZEN` Codex 進程內可自行 spawn 子代理，外層「單一 writer pool」零強制**（ID：`EXEC-NO-SUBAGENTS`；9/6 B5、9/14 N5；對應提案 `E2`；驗收 [`ACCEPTANCE-capability-boundary-2026-09-14.md`](ACCEPTANCE-capability-boundary-2026-09-14.md)） | **執行期（2026-09-14）**：read-only＋ephemeral 條件下存在能成功 spawn 的配置 **CONFIRMED**（`--disable multi_agent --disable apps --disable plugins` 變體：`tool_result spawn_agent success=true`、子代理回 PONG）；現行 baseline 只有失敗紀錄（`no thread with id`，原因未查）**UNVERIFIED**，失敗不是保護。**`--disable multi_agent` 不足以禁止 spawn**（gpt-6-astra）**CONFIRMED**：原始碼 `core/src/config/mod.rs:1564-1571` `multi_agent_version_for_model` 讓模型宣告（astra／sol／terra 宣告 v2，`codex debug models`）優先於 `features.multi_agent`；**9/6 B5「`--disable multi_agent` 已實測有效」只看了 `features list` 顯示值，已被推翻**。有效控制：`-c agents.enabled=false` → `MultiAgentVersion::Disabled` → `tools/spec_plan.rs:648-650` 不註冊 collaboration 工具（原始碼為主證據；兩次行為探測回 `TOOL_UNAVAILABLE` 為佐證）；對 `codex agents` 面板／review／exec 的副作用 **UNVERIFIED**。觸發面 **CONFIRMED**：全域 `~/.codex/AGENTS.md:23` 命中 `<multi_agent_mode>` 放行條件。該次子代理回報 `sandbox_policy=read-only`、`mcp_servers="node_repl"`（一次觀察，不宣稱通則）。上游 #33267 仍 open。 |
| <a id="CODE-MODE-EXEC-SURFACE"></a>**⬜ `UNVERIFIED` read-only consult 內 `functions.exec`（code-mode JS，經 `node_repl`）可執行 JS**（ID：`CODE-MODE-EXEC-SURFACE`；2026-09-14 驗收 §5） | 入口可用與 JS 執行 **CONFIRMED**（模型用它試 `tools.spawn_agent`，得到 `TypeError`）；檔案／網路／認證／副作用邊界 **UNVERIFIED**。`node_repl` 不受 `--disable apps --disable plugins` 影響。不足以支持解凍；本輪不深究。 |
| <a id="OUTFILE-CMDSAFE"></a>**🔴 `FROZEN` `codex-exec.ps1` 的 `-OutFile` 未經 `Assert-CmdSafePath` 就拼進 `cmd /c`**（ID：`OUTFILE-CMDSAFE`；9/6 A1） | 讀碼 **CONFIRMED**（`Dir`／`SchemaFile` 有檢查、`OutFile` 沒有）；低信任輸入是否可達此參數 **CANDIDATE**（Codex M1：不接受「呼叫端是 Claude」當低風險理由）。修法一行；升級為凍結例外須先證明可達性＋無害重現。 |
| <a id="SENDMESSAGE-SURFACE"></a>**🔴 `FROZEN` `SendMessage`（跨 session 外發，CC 2.1.224+）不在 settings matcher 也不在 `MUTATING_BUILTIN`**（ID：`SENDMESSAGE-SURFACE`；9/6 B2；對應 `E7`） | **CONFIRMED**（`windows/settings.snippet.json:6` 與 hook 皆無）。姿態 A 下 gate 本非邊界，屬風險面擴大；README／SKILL 已於 9/14 改用它當「未納管」範例。落地含 live settings 同步與 `matcher-contract --live`。 **2026-09-23 複核：** B20 在 CC 2.1.280 仍成立；三平台：macOS ✔（`macos/settings.snippet.json:6`）／Linux ✔（`linux/settings.snippet.json:6`）／Windows ✔（`windows/settings.snippet.json:6`），hook 清單為 macOS／Linux :51、Windows :41，均未列 `SendMessage`。來源：`A0923-B20`；macOS 探針 **CONFIRMED**，其餘讀碼確認、未 live。 |
| <a id="MCP-PREVIEW-START"></a>**🔴 `FROZEN` `mcp__Claude_Browser__preview_start`／`preview_stop` 因名字含 `view` 命中唯讀字樣而放行**（ID：`MCP-PREVIEW-START`；9/6 B4） | **CONFIRMED**（hook `MCP_READ_RE` 含 `view`、寫入字樣無 `start`）。最小修法＝`MCP_FORCE_GATE_ACTION` 加兩個名字；Z 案（action 分詞）另議。 **2026-09-23 複核：** B20 在 CC 2.1.280 仍成立；三平台：macOS ✔（`macos/hooks/super-mode-consult-gate.js:311`）／Linux ✔（`linux/hooks/super-mode-consult-gate.js:311`）／Windows ✔（`windows/hooks/super-mode-consult-gate.js:300`）。來源：`A0923-B20`；macOS 探針 **CONFIRMED**，其餘讀碼確認、未 live；server 名污染的另一入口另見 [`MCP-SERVER-READ-MATCH`](#MCP-SERVER-READ-MATCH)。 |
| <a id="GATE-QUOTE-LEXER"></a>**🔴 `FROZEN` gate 以 `[;\|&\r\n]` 切段、不辨識引號 → `grep -E 'a\|b'`、`for…do…done`、`grep "=>"` 等唯讀指令被攔**（ID：`GATE-QUOTE-LEXER`；9/6 C1；對應 `E5`） | **CONFIRMED**（9/6 三次親踩；hook :624-635 段）。每次誤攔逼一次諮詢或誘使關 gate。需 quote-aware lexer＋`gate-cases` 新案，三平台。 **2026-09-23 複核：** B01 的新後果是拒絕訊息要求的 `codex-consult.sh -d "/path/to/proj&x"` 本身也被擋，形成鑄證死循環；commit `25a054b` 已在 POSIX SKILL 的 §3.5 連至 orchestration 脫困說明（先確認 cwd 再用 `-d .`），Windows 不宣稱 `-Dir .` 等價（`.ps1` 原樣寫入 repo，且 `Assert-CmdSafePath` 另拒 `&`）；三平台：macOS ✔（`macos/hooks/super-mode-consult-gate.js:275`）／Linux ✔（`linux/hooks/super-mode-consult-gate.js:275`）／Windows ✔（`windows/hooks/super-mode-consult-gate.js:267`）；POSIX 唯讀切段另見同檔 :620、Windows :635。來源：`A0923-B01`；macOS 隔離探針 **CONFIRMED**，Linux／Windows **CONFIRMED（讀碼，未 live）**。 |
| <a id="ARTIFACT-ACTION"></a>**🔴 `FROZEN` `Artifact`／`RemoteTrigger` 的唯讀 action（list／read／comments／status／read_db…）也要憑證，且放行即降到 3 分鐘**（ID：`ARTIFACT-ACTION`；9/6 C2；對應 `E6`） | **CONFIRMED**（hook 不讀 `tool_input.action`；`CONSUMING_BUILTIN` 含 Artifact）。修法小：分支讀 `action`。 **2026-09-23 複核：** B20 在 CC 2.1.280 仍成立；`Artifact` 的 read/list/quickstart 都會把憑證降至 3 分鐘，`RemoteTrigger` 唯讀 action、`ExitWorktree {action:keep}` 與進入既有 worktree 也要憑證（後兩者不消耗憑證）；三平台：macOS ✔（`macos/hooks/super-mode-consult-gate.js:297`）／Linux ✔（`linux/hooks/super-mode-consult-gate.js:297`）／Windows ✔（`windows/hooks/super-mode-consult-gate.js:286`），consuming 清單為 macOS／Linux :64、Windows :54。來源：`A0923-B20`；macOS 探針 **CONFIRMED**，其餘讀碼確認、未 live。 |
| <a id="CODEX-CHECK-STAGE-COLUMN"></a>**🔴 `FROZEN` `codex-check.ps1` 解析 `features list` 只取首尾欄，`removed`＋`true` 被算成 ON**（ID：`CODEX-CHECK-STAGE-COLUMN`；9/6 D7；對應 `E8` 一部分） | **CONFIRMED**（`item_ids`、`collaboration_modes`、`unified_exec_zsh_fork` 等進漂移報告）；`guardian_approval` 列進高風險面屬輕微誤警。9/14 重設的 baseline 已含這些誤計列，修好後 baseline 要再重設一次。 |
| <a id="POSIX-UUIDGEN-PYTHON3"></a>**🔴 `FROZEN` POSIX 腳本三個實作不對稱：`codex-exec.sh` 硬依賴 `uuidgen`、`codex-consult.sh` 鑄證依賴 `python3`（README／AI-INSTALL 未列前置）、`codex-exec.ps1` schema 驗證仍用 `ConvertFrom-Json`**（ID：`POSIX-UUIDGEN-PYTHON3`；9/6 B8） | **CANDIDATE**（A4 agent 靜態閱讀，主線未重現）。 **2026-09-23 複核：** B21 將其中 python3 鑄證依賴升為 **CONFIRMED（macOS stub 重現）**：Codex 已跑完才發現缺 python3，或整份簡報經 `CONSULT_BRIEF` 超過環境大小限制（macOS 1,100,000 bytes 案例回 44；Linux 門檻未實測），卻提示查權限後重跑，會再耗額度；不把這項確認擴及本列其餘子項。三平台：macOS ✔（`macos/skills/超級模式/scripts/codex-consult.sh:257`）／Linux ✔（`linux/skills/超級模式/scripts/codex-consult.sh:257`，讀碼）／Windows ✘（`windows/skills/超級模式/scripts/codex-consult.ps1:392`，PowerShell 直接產 JSON，無 python3／簡報環境變數傳遞）；Linux／Windows 未 live。來源：`A0923-B21`。 |
| <a id="GATE-ENV-PREFIX"></a>**🟡 `ACCEPTED_RISK` gate 把 `env` 放進唯讀白名單 → `env rm -rf x`、`env node build.js` 整段放行**（ID：`GATE-ENV-PREFIX`；9/6 B3） | **CONFIRMED**，但**不是新發現**：[`super-mode-hardening-plan-2026-07.md:24`](super-mode-hardening-plan-2026-07.md) 已明文停止對 shell verb 做窮舉硬化（姿態 A 定案）。本列只是指回該裁決。CC 2.1.268 修了自家 `env -C`／`eval` 的 deny 繞過，與本 gate 無關。 |
| <a id="WORKER-CONFIG-PLANTING"></a>**⬜ `UNVERIFIED` workspace-write worker 可植入 `.codex/config.toml`（本 repo 在 Codex 是 `trusted`，家目錄整個 `trusted`），下一次派工即載入**（ID：`WORKER-CONFIG-PLANTING`；9/6 B7、9/14 Codex 補充） | **CANDIDATE**（官方 config-advanced：專案層可設 `[features]`、`[mcp_servers]`、network）。便宜對策屬 `DOC-FIX`：審查清單加「diff 含 `.codex/`、`hooks.json`、`AGENTS.md` ⇒ 一律人工看」。同一理由，9/14 提過的「本 repo 放 `.codex/config.toml` 關 multi_agent」**不採**：受控對象能改寫控制點。 |
| <a id="GLOBAL-AGENTS-DELEGATION"></a>**⬜ 使用者裁決（非 repo 變更）全域 `~/.codex/AGENTS.md:23` 主動委派規則**（ID：`GLOBAL-AGENTS-DELEGATION`；9/14 N5） | 該行對互動 Codex 可能是想要的，但它同時進每次 headless consult／exec。選項：改寫加限定（AGENTS.md 無法區分 headless，效果 **UNVERIFIED**）、留著等 `E2` 在腳本層關、或縮減使用範圍（敏感資料不餵 consult）。由使用者決定，不在 repo 內處理。 |

### B. 文件事實訂正待做（`DOC-FIX`，凍結允許）

| 項目 | 說明 |
|---|---|
| **✅ 已做（2026-09-14）D1 `orchestration.md` §3.5 里程碑諮詢範本缺首行裁決要求**（9/6 D1，high；範本補首行契約、明寫 `-NoCredential`／`-n` 與 schema 模式可省略，三平台） | 鑄證判準（`lib/consult-answer.js`）強制首行 `ALLOW:`／`BLOCK:`，照範本寫會 exit 43（9/6 親踩一次；「每次都白燒」是未量化推論）。修法：只在**會鑄證**的範本結尾固定加首行契約，**明確排除 `-NoCredential` 討論模式與 `-SchemaFile` 模式**；改前唯讀核對 parser 實際要求。三平台。 |
| **✅ 已做（2026-09-14）D2 事實半邊：「360000ms 前景跑」在長諮詢下不成立**（9/6 D2；三平台 SKILL §3 與本機 `~/.claude/CLAUDE.md` 補事實，流程半邊仍為 `E13`） | 事實：超時會被工具自動轉背景；**進行中逐字稿 0 bytes 屬正常**（stdout 只承載最終回覆），勿以空檔判失敗。只改事實敘述；「一律背景跑」是流程變更＝`E13`。 |
| **✅ 已做（2026-09-14）D9 `--disable remote_plugin` 被稱「硬化」屬高估**（9/6 D9；README、本檔「平台功能落差」表、`linux-platform-notes.md` 加訂正；`.ps1`／`.sh` 內文案未動） | 它只關遠端目錄，關不掉已裝外掛與 connector（上游 #28443）。README:48、本檔上方「平台功能落差」表、`linux-platform-notes.md:21` 的例子改成 `--disable apps --disable plugins`；`codex-check.ps1:227`／`codex-check.sh:248` 內的文案屬產品碼，只記不改。 |
| **✅ 已做（2026-09-14）D5 SKILL 本機備註寫 `codex.ps1`，實作是 `codex.cmd` 經 `cmd.exe /d /s /c`**（9/6 D5） | 只有 windows SKILL.md 末段有此誤寫，已訂正；macOS／Linux 的本機備註本來就沒寫錯。 |
| **D4 Windows `orchestration.md` §3.5 低報 hook 實際強制面**（9/6 D4） | 缺：自動執行檔名不豁免、唯讀 runner 指向暫存或 `~/.claude` 仍要憑證、憑證 JSON 含 repo 綁定、安全關鍵檔清單少 `.codex-check-baseline`；linux 版缺最後一點。CANDIDATE，改前逐行核對 hook。 |
| **D8 其他過時敘述**（9/6 D8；**2026-09-14 已做三條**：SKILL「Opus 5 起」「Sonnet 可／Haiku 不可」加 as-of、「hook 設定下個 session 才生效」改「官方稱即時載入、本 repo 未實測」、README「14 天」改成實際觸發條件；本檔 `WINDOWS-CI` 列亦已改） | **仍未做**（整潔度，可放著）：README「PowerShell 5.1」補「pwsh 7 為主要呼叫 host」不刪 5.1；linux orchestration 五態／`.codex-check-baseline`；windows orchestration「v3」vs hook 自標 v2；hook :121 註解矛盾（產品碼，只記）；resume 結論加版本註。 |
| <a id="transcript-cleanup-policy"></a>**逐字稿清理「14 天」政策未生效**（[`plugin-reeval-2026-08.md`](plugin-reeval-2026-08.md) A6） | 清理只在 `super-mode -Off` 路徑觸發，日常討論從不觸發；且無檔名白名單、錯誤靜默。**不手動清**（會刪掉 `CONNECTOR-EXPOSURE`／`EXEC-NO-SUBAGENTS` 的驗收證據，進行中檔可能 0 bytes）。README 的敘述**已於 2026-09-14 改成實際觸發條件**；SKILL 未提 14 天；產品化留 9/30。 **2026-09-23 複核：** 另有觸發清理時的資料遺失候選 [`OFF-CLEANUP-RECURSIVE`](#OFF-CLEANUP-RECURSIVE)（B28）；本列「日常討論不觸發」結論不變。 |
| **B9 一句：exec 回 46＝「結果不明，先查狀態」**（9/6 B9） | `codex-exec` exit 46 時 worker 可能已改檔；SKILL §3「派工失敗＝退回重派」要補「非零碼一律先核 `_last.txt`、`git status`／diff 與外部狀態，不自動重派」。三平台。 |
| **D3 本機 `~/.claude/CLAUDE.md` 與 repo `CLAUDE-global-rule.md` snippet 的規則同步**（9/6 D3） | 本機版有 `paused_unknown`／`disabled_quota` 三態與 9/14 新增的具名非配額原因；repo snippet 缺「四問」句與三態。屬**規則同步**，由使用者單獨決定方向（回灌 repo 或以 repo 為準）。 |
| <a id="LTS-DECISION-MEMO"></a>**9/30 決策備忘**（ID：`LTS-DECISION-MEMO`；9/14 Codex 排序第 3） | 題目：永久封存 vs 最低成本 LTS。要寫：LTS 最低支援的平台／版本、負責人、投入上限、退出條件；`WINDOWS-CI` 為必要非充分；「凍結禁 CI、解凍先要 CI」死結的解法（若選 LTS，明確授權一段有限的 CI 建置期；現在只設計與估算、不實作）。 |

### C. 提案（`PROPOSED`，只有 spec；9/30 裁決採不採。順序是 9/14 §6 的裁決排序，**不是排程**）

| ID | 提案 | 前置／驗證 | 來源 |
|---|---|---|---|
| `E3′` | exec 成功判定改四個獨立命題：程序與協定完成 ∧ 本次執行的新產物（非空、非拒答）∧ AC 逐條成立 ∧ 無阻擋 AC 的未解拒絕／錯誤；`--json` 事件名釘 codex 版本；驗收用 `git status --porcelain`＋diff | 上游 #45251（只有 app-server durable）；不預設 Codex 有 CC 的 `permissionDenials` | 9/6 E3、9/14 |
| `E2` | consult 加 **`-c agents.enabled=false`**（**不是** `--disable multi_agent`——對 astra／sol／terra 無效，2026-09-14 驗收推翻）；exec 是否同加另議（子代理對派工可能有用，且面板／review 副作用未驗） | 見 `EXEC-NO-SUBAGENTS`；consult 與 exec 分開裁決 | 9/6、9/14 N5、驗收 |
| `E1` | consult／exec 選配 `-Effort`（映射 `-c model_reasoning_effort=`），預設繼承；consult 明文禁 `ultra` | effort 是成本旋鈕，不代替 `E2` | 9/6、9/14 N2 |
| `E17` | worker 環境衛生：`SUPER_MODE_WORKER=1`、剝 `ANTHROPIC_*`；AGENTS.md 範本加「不得呼叫 claude／codex CLI」 | 多層防護之一，不宣稱禁止遞迴 | 9/14 |
| `E16` | gate scratchpad 豁免改讀 hook stdin `scratchpad_dir`（CC 2.1.257+），保留 TEMP fallback；路徑邊界照舊 | — | 9/14 |
| `E10′` | 派工簡報 Astra 條款逐條重評：限制配完成判準、刪「記得測試」樣板、「只跑指定測試；覆蓋不到 AC 就回 `UNVERIFIED`」、不疊人設／急迫語 | 純文件但屬行為變更 | 9/6 E10、9/14 N4 |
| `E14` | 審查型派工改用 `codex exec review --uncommitted --ephemeral --output-schema … -o …` 取代對停更 plugin 的依賴 | 先驗有效 sandbox、父命令 `--sandbox` 是否套用、遠端工具權限 | 9/14 |
| `E18` | consult 定位改「證據挑戰」；盲審用兩輪（第一輪不含 Claude 初判）；Claude 先落裁決再讀 Codex | 流程變更；日常試用只算探索 | 9/14 N3 |
| `E20` | 最小上下文配置的驗證：量測 worker 每次帶入的個人 skills 清單（~22KB）與全域 AGENTS.md 的實際 token／快取 | 先驗開關再寫解法 | 9/14 §5 |
| `E13` | consult 一律 `run_in_background`；須處理完成回收、取消、孤兒程序（CC 2.1.269 起背景 PowerShell 跨 session 存活） | 流程變更 | 9/6 D2、9/14 |
| `E19` | 派工選配 `--worktree`（feature `worktrees` 仍 experimental）；trust／憑證／遠端權限另驗 | — | 9/14 |
| `E4` | repo `.codex/hooks.json` PreToolUse 在 worker 進程內強制寫入範圍 | 需一次互動授信；hook 自身 fail-open（#45293）；未信任 hook 有無 stderr 未驗 | 9/6、9/14 |
| `E5`／`E6`／`E7` | 對應 `GATE-QUOTE-LEXER`／`ARTIFACT-ACTION`／`SENDMESSAGE-SURFACE` 的修法 | — | 9/6 |
| `E8` | `codex-check` 盤點已連結 connector、保留 features 狀態欄、`plugin list --json`；加 session 期間外掛刷新註記 | 對應 `CODEX-CHECK-STAGE-COLUMN` | 9/6、9/14 |
| `E9` | AGENTS.md 生成前置檢查：無 `AGENTS.override.md`、合計 <32KiB、提醒全域 `~/.codex/AGENTS.md` 會前置；不疊人設／急迫語；schema gate | 純文件 | 9/6、9/14 |
| `E11` | exec 加 `--ephemeral`（rollouts 佔磁碟；resume 本就不可用） | — | 9/6 |
| `E12` | hook 拒絕訊息依 `agent_id`／`agent_type` 分流 | — | 9/6 |

## 超級模式稽核登錄（2026-09-23；來源：多代理稽核＋Codex 反方）

本節只做文件事實訂正與 backlog 維護，不代表授權產品變更。來源代號：`A0923-Bnn`＝2026-09-23 多代理稽核第 nn 條；`C0923`＝同日 Codex 反方諮詢（A1–A5／B1–B4 為其段落編號）；`E0923`＝同日 execpolicy／baseline 初判。原始紀錄留在維護者本機、未入 repo；不將私人附件或本機 app 名單提交公開 repo。`A0923` 的受驗對象是已安裝 macOS 版（依交付說明，內容等同本分支 `macos/`）；初判衝突處以 `C0923` 與本次使用者最終裁決為準。

證據狀態：**CONFIRMED**＝指定範圍有讀碼或重現證據；**CANDIDATE**＝機制／來源支持但影響未 live 重現；**UNVERIFIED**＝未驗。除特別標 macOS live 外，B 編號的重現是附件既有的隔離 HOME／stub／gate 探針，不是本輪重新執行。本輪僅讀 repo 碼、核對文件。三平台欄 ✔＝列出的機制／曝露存在，✘＝該機制不存在（不代表整體無其他問題），?＝讀碼不足以判斷；Linux／Windows 均未 live，局部成立會明寫。腳本欄的同列簡名沿用前一個完整 `scripts/` 路徑。

依 [姿態 A](super-mode-hardening-plan-2026-07.md)，gate 不是安全邊界；B02、B04–B10、B18 等漏判／誤判均為 `FROZEN`，不據此主張 security 解凍。本次只有 `EXECPOLICY-ALLOW-INHERIT`（資格成立）、`OFF-CLEANUP-RECURSIVE`（資料遺失候選）、`CC-AGENTS-MD`（上游候選）進入解凍資格討論。

### A. 缺陷／曝露（產品碼，凍結中）

| 項目 | 證據狀態、影響與處置 | 三平台 | 來源 |
|---|---|---|---|
| <a id="EXECPOLICY-ALLOW-INHERIT"></a>**🔴 `FROZEN` consult／exec 繼承 execpolicy allow 的沙箱外權限（security，已出貨）**（ID：`EXECPOLICY-ALLOW-INHERIT`） | **CONFIRMED（macOS live，2026-09-23，codex-cli 0.156.1）**：與 consult 相同旗標、`approval_policy=never`；對照直接寫 workspace 外檔案遭 `PermissionError`，`python -m pytest` 卻讓 conftest 寫出外部檔案，`git add` 改動 index。三平台 consult 與 main 相同且未帶 `--ignore-rules`；Linux／Windows **CONFIRMED（讀碼，未 live）**；workspace-write exec 同樣繼承。定位是**產品整合對權限的錯誤假設，不是 Codex 本身漏洞**（allow 的沙箱外語義是官方行為）。`prefix_rule` 只比對 argv 前綴，後續參數不限；`match`／`not_match` 只是載入時的測試案例。例如 `git restore --staged --worktree .` 命中 `git restore --staged` 的 allow，不命中 `git restore --worktree` 的 prompt；`git ls-remote --upload-pack=<程式>` 可指定程式；`python -m pytest --basetemp=<既有目錄>` 在 base temp 初始化時遞迴刪除該目錄（須有刪除權限；pytest 拒絕 cwd 及其祖先）。**UNFREEZE 資格成立，是否解凍由使用者決定**；維持 `FROZEN`。修法 `--ignore-rules` **UNVERIFIED**：也移除 prompt 在 approval=never 下形成的拒絕，且管不到 MCP／apps／hooks。本機止血＝收窄使用者自己的 rules（非 repo 變更，見 C）。 | macOS ✔（`macos/skills/超級模式/scripts/codex-consult.sh:183`、`codex-exec.sh:89`／:102）／Linux ✔（`linux/skills/超級模式/scripts/codex-consult.sh:183`、`codex-exec.sh:89`／:102）／Windows ✔（`windows/skills/超級模式/scripts/codex-consult.ps1:285`、`codex-exec.ps1:167`） | `E0923` live 紀錄＋`C0923` A1–A5＋使用者最終裁決；[官方 rules](https://learn.chatgpt.com/docs/agent-configuration/rules)；[pytest](https://docs.pytest.org/en/stable/how-to/tmp_path.html) |
| <a id="GATE-SAFE-PREFIX"></a>**🔴 `FROZEN` 腳本放行 regex 接受任意動詞前綴（HIGH）**（ID：`GATE-SAFE-PREFIX`） | **CONFIRMED**：`CODEX_SAFE_RE` 允許空白與任意字詞，破壞性檢查只看腳本名之後；`git commit …/codex-check.sh`、`rm …/super-mode.sh` 等免憑證且跳過收尾降級，exec 簡報名含 safe 腳本名也可誤放。反向 `bash "<consult 路徑>"` 會誤擋；Windows 對應 `.ps1` 前綴也未限動詞。屬姿態 A 的紀律失效，非 security。 | macOS ✔（`macos/hooks/super-mode-consult-gate.js:69`）／Linux ✔（`linux/hooks/super-mode-consult-gate.js:69`）／Windows ✔（`windows/hooks/super-mode-consult-gate.js:59`）；放行尾段為 macOS／Linux :272–277、Windows :264–269。 | `A0923-B02` |
| <a id="GATE-SCOPE-CROSS-SESSION"></a>**🔴 `FROZEN` scope 不能隔離 session，會互搶全機憑證（HIGH）**（ID：`GATE-SCOPE-CROSS-SESSION`） | **CONFIRMED**：scope 外一般檔案工具與唯讀 shell 可放行，非唯讀 shell／Monitor 仍要憑證；MCP／外發內建工具不看 scope。憑證綁別的 repo 會拒絕；其他 session 按拒絕訊息諮詢或 off，會覆寫同一憑證或關掉全機 gate。文件半邊已由 `25a054b` 訂正；產品行為維持凍結。 | macOS ✔（`macos/hooks/super-mode-consult-gate.js:268`）／Linux ✔（`linux/hooks/super-mode-consult-gate.js:268`）／Windows ✔（`windows/hooks/super-mode-consult-gate.js:260`）；repo 比對 macOS／Linux :381、Windows :370，Monitor macOS／Linux :239、Windows :231。 | `A0923-B03` |
| <a id="GATE-GH-API-METHOD"></a>**🔴 `FROZEN` gh api 隱含 POST 與等號 method 漏判（MEDIUM）**（ID：`GATE-GH-API-METHOD`） | **CONFIRMED**：只檢查明寫方法的形式，`-f`／`-F`／`--field`／`--raw-field`／`--input` 觸發的 POST、GraphQL mutation、`--method=DELETE` 等被當唯讀；相同動作明寫 `-X POST` 則攔截。 | macOS ✔（`macos/hooks/super-mode-consult-gate.js:123`）／Linux ✔（`linux/hooks/super-mode-consult-gate.js:123`）／Windows ✔（`windows/hooks/super-mode-consult-gate.js:112`）；DESTRUCTIVE 為 macOS／Linux :86、Windows :75。 | `A0923-B04` |
| <a id="GATE-CONSUMING-FALSE-POSITIVE"></a>**🔴 `FROZEN` 唯讀查詢誤觸收尾降級（MEDIUM）**（ID：`GATE-CONSUMING-FALSE-POSITIVE`） | **CONFIRMED**：`git merge-base` 的連字號仍符合 `merge` 後的字界；grep／rg／git log 搜尋字串含 `git push`、`git commit` 也命中全指令 regex，無憑證拒絕、有憑證降至 3 分鐘。其他破壞性字樣搜尋也誤擋。B05 的 [`merge-tree --write-tree`](https://git-scm.com/docs/git-merge-tree) 會寫 Git objects，不能當純唯讀證據；這不影響 merge-base／搜尋字串案例。 | macOS ✔（`macos/hooks/super-mode-consult-gate.js:97`）／Linux ✔（`linux/hooks/super-mode-consult-gate.js:97`）／Windows ✔（`windows/hooks/super-mode-consult-gate.js:86`）；降級 macOS／Linux :390、Windows :379。 | `A0923-B05`（唯讀定性依實際命令收斂） |
| <a id="GATE-CONSUMING-MISS"></a>**🔴 `FROZEN` Git 全域選項與其他 publish 指令未降級（MEDIUM）**（ID：`GATE-CONSUMING-MISS`） | **CONFIRMED**：`git -C`／`--no-pager`／`-c` 後的 commit/push、`gh release create`／`uv publish`／`twine upload`／`poetry publish`／`docker push` 有憑證可放行但不降至 3 分鐘；`git -C <另一 repo> push` 只比 cwd，未綁 operand repo。 | macOS ✔（`macos/hooks/super-mode-consult-gate.js:97`）／Linux ✔（`linux/hooks/super-mode-consult-gate.js:97`）／Windows ✔（`windows/hooks/super-mode-consult-gate.js:86`）；cwd 綁定 macOS／Linux :269、Windows :261。 | `A0923-B06` |
| <a id="GATE-READONLY-WRITE-FLAGS"></a>**🔴 `FROZEN` 唯讀白名單忽略顯式寫入參數（MEDIUM）**（ID：`GATE-READONLY-WRITE-FLAGS`） | **CONFIRMED**：`npm run lint:fix`／`lint -- --fix`／`test -- -u`、`cargo clippy --fix`、`go env -w`、`git reflog expire/delete`、`git diff/log --output` 等被放行；重導向剝除式 `nul` 的字界也吃進 `nul.txt`。這是明示寫入參數的漏判，與已接受的 runner 子程序副作用分開記錄，仍非安全邊界。 | macOS ✔（`macos/hooks/super-mode-consult-gate.js:106`）／Linux ✔（`linux/hooks/super-mode-consult-gate.js:106`）／Windows ✔（`windows/hooks/super-mode-consult-gate.js:95`）；runner 清單 macOS／Linux :111–112、Windows :100–101；nul 剝除 macOS／Linux :616、Windows :631。 | `A0923-B07` |
| <a id="MCP-SERVER-READ-MATCH"></a>**🔴 `FROZEN` MCP server 名污染唯讀比對（MEDIUM）**（ID：`MCP-SERVER-READ-MATCH`） | **CONFIRMED**：`MCP_READ_RE` 比完整工具名，server 名含 `view` 的 `mcp__pdf-viewer__interact` 可將 save_as／fill_form 判為唯讀（附件 schema 支持寫檔能力，未宣稱實際覆寫過 PDF）；`ccd_view` 等同型。與 [`MCP-PREVIEW-START`](#MCP-PREVIEW-START) 的 action 名含 view 分開追蹤；同屬姿態 A／Z 案已知 substring 殘餘的具體曝露。 | macOS ✔（`macos/hooks/super-mode-consult-gate.js:311`）／Linux ✔（`linux/hooks/super-mode-consult-gate.js:311`）／Windows ✔（`windows/hooks/super-mode-consult-gate.js:300`） | `A0923-B08` |
| <a id="ARTIFACT-SPLIT-SURFACE"></a>**🔴 `FROZEN` ArtifactData／ArtifactComments 進 hook 後放行（MEDIUM）**（ID：`ARTIFACT-SPLIT-SURFACE`） | **CONFIRMED**：matcher 的 unanchored `Artifact` 可命中兩個新名字，但 `MUTATING_BUILTIN.includes(tool)` 精確比對不含它們，因此 shared DB set/update/delete/batch、reply/resolve 與 out_dir 寫本機檔均無 gate 要求。不能只靠既有 [`ARTIFACT-ACTION`](#ARTIFACT-ACTION) 的 action 分支涵蓋；「何時從 Artifact 拆出」屬推論，未當版本事實。 | macOS ✔（`macos/hooks/super-mode-consult-gate.js:297`）／Linux ✔（`linux/hooks/super-mode-consult-gate.js:297`）／Windows ✔（`windows/hooks/super-mode-consult-gate.js:286`）；三份 settings.snippet.json:6 的 matcher 皆含 Artifact。 | `A0923-B09` |
| <a id="MCP-HARDDENY-RETRY"></a>**🔴 `FROZEN` MCP harddeny 的無憑證訊息誤導先諮詢（MEDIUM）**（ID：`MCP-HARDDENY-RETRY`） | **CONFIRMED**：不符合 pathbound policy／pathless allow 的 MCP 寫入或未知工具，有憑證仍 harddeny；但無憑證先回「諮詢成功後重試」，多耗一次諮詢才看見硬拒。SKILL／orchestration 的「要憑證」未交代此限制（文件殘餘登錄，這次不擴大 DOC-FIX）。 | macOS ✔（`macos/hooks/super-mode-consult-gate.js:339`）／Linux ✔（`linux/hooks/super-mode-consult-gate.js:339`）／Windows ✔（`windows/hooks/super-mode-consult-gate.js:328`）；harddeny macOS／Linux :355、Windows :344；通用訊息 macOS／Linux :394、Windows :383。 | `A0923-B10` |
| <a id="CREDENTIAL-REPO-ALIAS"></a>**🔴 `FROZEN` 憑證 repo 字串與實體目錄別名不一致（MEDIUM）**（ID：`CREDENTIAL-REPO-ALIAS`） | **CONFIRMED（macOS stub／探針）**：consult 用邏輯 pwd，hook 以字串比較 repo／原始動作路徑，symlink 或 NFC/NFD 同目錄可能被當不同專案，重諮詢同一拼法仍失敗。Linux 同樣未 realpath／Unicode 正規化；Windows 將 `$Dir` 原樣寫入（只修尾斜線），hook 也只做字串正規化，別名比對機制確認、實際 junction／Unicode 情境未 live。 | macOS ✔（`macos/skills/超級模式/scripts/codex-consult.sh:253`、`macos/hooks/super-mode-consult-gate.js:381`／:431）／Linux ✔（`linux/skills/超級模式/scripts/codex-consult.sh:253`、`linux/hooks/super-mode-consult-gate.js:381`／:431）／Windows ✔（`windows/skills/超級模式/scripts/codex-consult.ps1:250`／:392、`windows/hooks/super-mode-consult-gate.js:370`／:424；僅字串綁定機制） | `A0923-B11` |
| <a id="TOGGLE-ARG-SILENT"></a>**🔴 `FROZEN` POSIX 開關誤拼參數靜默成功（MEDIUM）**（ID：`TOGGLE-ARG-SILENT`） | **CONFIRMED**：`on --scope=DIR` 或位置參數被忽略，啟用 GLOBAL；`Off`／`of`／`--off` 落入 status catch-all，rc=0 且不關閉。B14 文件已訂正，產品 parser 未改。 | macOS ✔（`macos/skills/超級模式/scripts/super-mode.sh:18`／:37）／Linux ✔（`linux/skills/超級模式/scripts/super-mode.sh:18`／:37）／Windows ?（`windows/skills/超級模式/scripts/super-mode.ps1:13` 用 PowerShell switch 參數，沒有 POSIX catch-all；不同錯拼的參數繫結／多餘引數效果未 live，不套用 bash 結論） | `A0923-B12` |
| <a id="POSIX-CANCEL-CLEANUP"></a>**🔴 `FROZEN` consult／exec 中斷無 trap，暫存與子程序可能殘留（MEDIUM／LOW）**（ID：`POSIX-CANCEL-CLEANUP`） | **CONFIRMED（macOS stub）**：B13 exec 的 stderr 正常結束才併 log，取消可留 0 bytes／缺 STDERR 區段；只殺 wrapper 時 worker 仍可能寫檔。B22 consult 同樣無 cleanup trap，brief／answer／stderr 留在 TMPDIR，單殺 wrapper 時 codex／tee 可跑完但不鑄證；log 的 14 天清理不涵蓋 TMPDIR。合併相同生命週期根因，與流程提案 `E13` 互相關聯，未落實取消流程。 | macOS ✔（`macos/skills/超級模式/scripts/codex-exec.sh:75`／:134、`codex-consult.sh:164`／:183）／Linux ✔（`linux/skills/超級模式/scripts/codex-exec.sh:75`／:134、`codex-consult.sh:164`／:183）／Windows ?（`windows/skills/超級模式/scripts/codex-exec.ps1:217`、`codex-consult.ps1:351` 有 finally 清 brief／stderr，但強制終止下是否執行及子程序存活未驗，不能照抄「無 trap」結論） | `A0923-B13`、`A0923-B22` |
| <a id="DESIGNSYNC-TASKSTOP-SURFACE"></a>**🔴 `FROZEN` DesignSync／TaskStop 不在 matcher（LOW）**（ID：`DESIGNSYNC-TASKSTOP-SURFACE`） | **CONFIRMED**：matcher 不命中，hook 清單也未列；DesignSync 的 write_files／delete_files 等有遠端副作用，TaskStop 可中斷派工。附件 schema 指出 DesignSync 寫入須 finalized plan，屬降低曝露的條件；僅登錄原 plan 的遺漏，未擴充納管面。 | macOS ✔（`macos/settings.snippet.json:6`）／Linux ✔（`linux/settings.snippet.json:6`）／Windows ✔（`windows/settings.snippet.json:6`）；hook 清單 macOS／Linux :51、Windows :41。 | `A0923-B18`；`opus5-alignment-plan-2026-07.md` P2-B1 |
| <a id="GATE-READONLY-GAPS"></a>**🔴 `FROZEN` 常見唯讀入口未列入白名單（LOW）**（ID：`GATE-READONLY-GAPS`） | **CONFIRMED**：`awk`、`realpath`、`command -v`、`shasum`、`git --version`、`git stash show`、`uv run pytest`／`uv run ruff check`、環境賦值前綴等誤擋。與 [`GATE-ENV-PREFIX`](#GATE-ENV-PREFIX) 的誤放行不同，另列可用性缺口；引號切段部分已歸 [`GATE-QUOTE-LEXER`](#GATE-QUOTE-LEXER)。不因此重啟 shell verb 窮舉硬化。 | macOS ✔（`macos/hooks/super-mode-consult-gate.js:105`）／Linux ✔（`linux/hooks/super-mode-consult-gate.js:105`）／Windows ✔（`windows/hooks/super-mode-consult-gate.js:94`）；default-deny macOS／Linux :294、Windows :283。 | `A0923-B19`；姿態 A |
| <a id="CLI-ARG-ERROR-CONTRACT"></a>**🔴 `FROZEN` consult／exec 用法錯誤訊號與失敗摘要不一致（LOW）**（ID：`CLI-ARG-ERROR-CONTRACT`） | **CONFIRMED**：B23 consult、B27 exec 的值旗標放最後，`shift 2` 在 `set -e` 下靜默 rc=1；brief 不存在則 cat 原始錯誤＋rc=1，與 Codex 原生失敗撞號。exec 非零摘要漏 `_last.txt` 路徑（三平台皆同）；Windows 沒有 bash shift 路徑，不能套用其靜默缺值結論。與既有 `EXITCODE-CONTRACT` 已修的 capture／log 失敗路徑不同，不重開其結論。 | macOS ✔（`macos/skills/超級模式/scripts/codex-consult.sh:19`／:32、`codex-exec.sh:16`／:144）／Linux ✔（`linux/skills/超級模式/scripts/codex-consult.sh:19`／:32、`codex-exec.sh:16`／:144）／Windows ✔（`windows/skills/超級模式/scripts/codex-exec.ps1:231`，僅失敗摘要；參數宣告 :25、consult.ps1:27 是 PowerShell 機制） | `A0923-B23`、`A0923-B27` |
| <a id="CREDENTIAL-SESSION-ENV"></a>**🔴 `FROZEN` POSIX 憑證 session 讀舊環境變數（LOW）**（ID：`CREDENTIAL-SESSION-ENV`） | **CONFIRMED（附件 macOS CC 2.1.280 desktop／SDK）**：環境提供 `CLAUDE_CODE_SESSION_ID`，腳本卻讀 `CLAUDE_SESSION_ID`，未另設舊名時記成 unknown。hook 現在不讀 session，不影響目前放行；只損失 audit 線索，未來 session 綁定也不能直接信任此欄位。 | macOS ✔（`macos/skills/超級模式/scripts/codex-consult.sh:257`／:265）／Linux ✔（`linux/skills/超級模式/scripts/codex-consult.sh:257`／:265，讀碼，環境未 live）／Windows ✘（`windows/skills/超級模式/scripts/codex-consult.ps1:392`，憑證只含 repo／ts，沒有 session 欄或這個環境變數讀取） | `A0923-B24` |
| <a id="GATE-SCOPE-TRIM"></a>**🔴 `FROZEN` scope 尾端空白被 trim 成別的目錄（LOW）**（ID：`GATE-SCOPE-TRIM`） | **CONFIRMED（macOS 探針）**：on 原樣寫入目錄尾端空白，readScope 卻 trim；一般 Write／Edit 被誤認 scope 外免憑證。這個結果不擴張成「所有 shell 都不強制」（B03 非唯讀 shell 仍受管）。 | macOS ✔（`macos/skills/超級模式/scripts/super-mode.sh:23`、`macos/hooks/super-mode-consult-gate.js:413`）／Linux ✔（`linux/skills/超級模式/scripts/super-mode.sh:23`、`linux/hooks/super-mode-consult-gate.js:413`）／Windows ?（`windows/skills/超級模式/scripts/super-mode.ps1:21`／:25、`windows/hooks/super-mode-consult-gate.js:406` 也 trim，但 Resolve-Path／NTFS 尾空白可達性未驗） | `A0923-B25` |
| <a id="CODEX-CHECK-EXEC-DEPENDENCIES"></a>**🔴 `FROZEN` 相容性探測未覆蓋實際 exec 依賴（LOW）**（ID：`CODEX-CHECK-EXEC-DEPENDENCIES`） | 缺口 **CONFIRMED（讀碼）**；未來上游移除時的 breakage **CANDIDATE**，不是現行故障：macOS required_flags／smoke 未驗 `--disable remote_plugin` 與 `memories.*` 鍵；未知 feature 可讓 exec 失敗，記憶鍵改名可能靜默忽略。Linux／Windows exec 沒帶 remote_plugin，但同樣使用 memories 鍵且 smoke 未驗。另列而不併 `E8`：E8 處理能力盤點內容／輸出格式，本列處理實際呼叫依賴的相容性。 | macOS ✔（`macos/skills/超級模式/scripts/codex-check.sh:35`、`codex-exec.sh:89`／:90）／Linux ✔（`linux/skills/超級模式/scripts/codex-check.sh:74`／:81、`codex-exec.sh:90`；只對 memories 缺口）／Windows ✔（`windows/skills/超級模式/scripts/codex-check.ps1:28`／:470、`codex-exec.ps1:167`；只對 memories 缺口） | `A0923-B26`；`E8` 關聯但不重複 |
| <a id="OFF-CLEANUP-RECURSIVE"></a>**🔴 `FROZEN` off 遞迴清理非逐字稿與驗收證據（資料遺失候選）**（ID：`OFF-CLEANUP-RECURSIVE`） | **CONFIRMED（macOS 暫存重現＋附件 2026-09-23 唯讀盤點）**：`find -type f -mtime +14 -delete` 未限檔名、遞迴進子目錄、錯誤靜默；當時 104 份檔案中的 83 份符合清理條件（時間快照，未在本輪重掃或刪除）。Linux **CONFIRMED（讀碼，未 live）**。Windows 只清頂層、同樣無檔名白名單，不能稱遞迴。**UNFREEZE 候選（資料遺失）**，是否解凍待使用者決定。與上節[逐字稿清理「14 天」政策未生效](#transcript-cleanup-policy)互引：日常討論不觸發與一旦 off 刪太廣是兩個不同結論。 | macOS ✔（`macos/skills/超級模式/scripts/super-mode.sh:33`）／Linux ✔（`linux/skills/超級模式/scripts/super-mode.sh:33`）／Windows ✘（`windows/skills/超級模式/scripts/super-mode.ps1:35`，`Get-ChildItem -File` 無 `-Recurse`，:36–37 仍依年齡刪除所有頂層檔；未 live） | `A0923-B28`＋使用者裁決；既有 9/14 DOC-FIX 清理列 |
| <a id="CC-AGENTS-MD"></a>**🔴 `FROZEN` Claude 讀取 worker 專用 AGENTS.md（上游 breakage 候選）**（ID：`CC-AGENTS-MD`） | **CANDIDATE（官方文件＋使用者提供的 2.1.277／2026-09-18 changelog 裁決，未 live）**：支援此功能的 session 預設在 cwd 及上層都沒有 CLAUDE.md／.claude/CLAUDE.md／CLAUDE.local.md 時，啟動載入每層 AGENTS.md（含 .claude/AGENTS.md）；使用者層 `~/.claude/CLAUDE.md` 不算。生成範本的「不得做架構決策」會成為 orchestrator 自己的指示，且 workspace-write worker 能改檔。**UNFREEZE 候選（上游）**，未授權修流程。重現須分三組：①只有 AGENTS → 預設讀入；②已有計入檢查的 CLAUDE → 預設不直接讀 AGENTS（排除 import 等既有途徑）；③設定 `claude-md-and-agents-md` → 兩者皆讀。須記錄功能可用性，首次升級 session／feature flags／停用 builtin plugin 等會影響結果。 | macOS ✔（`macos/skills/超級模式/references/orchestration.md:16` 生成入口）／Linux ✔（`linux/skills/超級模式/references/orchestration.md:16`）／Windows ✔（`windows/skills/超級模式/references/orchestration.md:16`）；✔ 只確認三份範本都有生成面，載入影響三平台均未 live。 | 使用者裁決；[官方 memory／AGENTS.md](https://code.claude.com/docs/en/memory#agents-md)；[官方 changelog](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)（版本日期採提供的裁決） |
| <a id="CODEX-CHECK-HOOKS-INVENTORY"></a>**🔴 `FROZEN` 空 hooks.state 的保守盤點阻擋 baseline 更新（LOW，可用性）**（ID：`CODEX-CHECK-HOOKS-INVENTORY`） | **CONFIRMED（macOS 附件＋讀碼；Windows 讀碼）**：base config 的 `[hooks.state]` 表確實為空，但**有效 hook 集合未知**。程式刻意以「無法證明有效集合為空」回 UNPARSEABLE，故要求更新會 REFUSED；不是單純 parser bug，不能把空表直接改判 EMPTY，也不以刪表／手改 baseline 消警示。未覆蓋來源含 hooks.json／project／profile／managed／plugin／`-c`。Linux 沒有這項能力面 baseline／hooks 盤點，是既有落差，不延伸 parity 待辦。 | macOS ✔（`macos/skills/超級模式/scripts/codex-check.sh:159`／:165／:318）／Linux ✘（`linux/skills/超級模式/scripts/codex-check.sh:7`／:74，只有版本／smoke，無 baseline 盤點）／Windows ✔（`windows/skills/超級模式/scripts/codex-check.ps1:153`／:161／:268；未 live） | `C0923` B3＋使用者最終裁決（取代初判與附件末尾被否決的 parser-bug 定性） |

### B. DOC-FIX（文件事實訂正；另列未做的流程缺陷）

| 項目 | 說明 | 來源 |
|---|---|---|
| **✅ 已做（2026-09-23）`DOC-FIX` B14 POSIX 殘留 Windows 旗標** | macOS／Linux SKILL 與 orchestration 改用 `off`、`-n`、`-s`；Windows 原生旗標不適用此修正。 | commit `25a054b`（已核 `git show --stat`）；B14 |
| **✅ 已做（2026-09-23）`DOC-FIX` B15 planner 死引用** | 三平台 SKILL §1 改為內建 Plan subagent／手寫；此完成項只涵蓋死引用，B15 對其他 spec 搜尋慣例的觀察不等於已調整流程。 | commit `25a054b`；B15 |
| **✅ 已做（2026-09-23）`DOC-FIX` B01 引號內特殊字元與脫困法** | 三平台訂正引號不保護 gate 字元比對；POSIX 在確認 cwd 後用 `-d .`，Windows 未宣稱 `-Dir .` 可等價脫困。產品缺陷併 [`GATE-QUOTE-LEXER`](#GATE-QUOTE-LEXER)。 | commit `25a054b`；B01 |
| **✅ 已做（2026-09-23）`DOC-FIX` B03 scope 的實際語義** | 三平台 SKILL／orchestration 訂正 scope 非 session 隔離、非唯讀 shell／Monitor 與 pathless 工具仍受管；產品列見 [`GATE-SCOPE-CROSS-SESSION`](#GATE-SCOPE-CROSS-SESSION)。 | commit `25a054b`；B03 |
| **✅ 已做（2026-09-23）`DOC-FIX` B16 writer pool 的 worktree 參數** | 三平台 orchestration §5 說明 `isolation: 'worktree'` 屬 Claude 子代理；orchestrator 建獨立 git worktree，以 `-d`／`-Dir` 指向，腳本未接 Codex 0.156 experimental `--worktree`。 | B16；本次任務 2.1 |
| **✅ 已做（2026-09-23）`DOC-FIX` B30 review schema 解析基準** | 三平台 orchestration §5 範例改已安裝位置的絕對路徑；sh :40–41 的檔案檢查／pwd 與 ps1 :96–97 的 Test-Path／Resolve-Path 都相對呼叫端 cwd，不是 skill 根或 worker 目錄。歷史規劃文件的舊範例不在本次可改範圍。 | B30；本次任務 2.2 |
| **✅ 已做（2026-09-23）`DOC-FIX` AGENTS.md 載入影響** | 三平台 SKILL §2 與 orchestration §2 加官方事實與 [`CC-AGENTS-MD`](#CC-AGENTS-MD) 連結，只寫影響，未新增流程規定。 | 使用者裁決；本次任務 2.3 |
| **✅ 已做（2026-09-23）`DOC-FIX` B29 安裝位置的斷鏈／平台狀態引用** | 三平台 SKILL 與 orchestration 的 repo docs／README 引用改 GitHub 絕對 URL，平台狀態直接指 README「功能差距」；保留 skill 內部 references 引用。 | B29；本次任務 2.4 |
| **✅ 已做（2026-09-23）`DOC-FIX` consult 唯讀宣稱** | 三平台 SKILL／orchestration 與 README 說明 sandbox 只約束受保護指令、命中 execpolicy allow 會在沙箱外執行；MCP／apps／hooks 不能由此推論無副作用。見 [`EXECPOLICY-ALLOW-INHERIT`](#EXECPOLICY-ALLOW-INHERIT)。 | `C0923` A3＋最終裁決；本次任務 2.5 |
| <a id="SUBAGENT-RULE-DELIVERY"></a>**🔴 `FROZEN` B17 子代理禁諮詢／派工規則缺送達機制**（ID：`SUBAGENT-RULE-DELIVERY`；流程缺陷，未做） | **CONFIRMED（讀碼／附件 gate 探針）**：三份 SKILL §5 有禁令，但未要求把它送進各子代理 prompt；safe consult 放行時不會顯示拒絕訊息的禁令，有共用憑證的 exec 也不分 agent。附件的 Workflow 指示衝突可能使子代理自行諮詢，實際自主觸發 **CANDIDATE**；本輪未讀本機 CLAUDE.md。三平台：macOS ✔（`macos/hooks/super-mode-consult-gate.js:272`／:398）／Linux ✔（`linux/hooks/super-mode-consult-gate.js:272`／:398）／Windows ✔（`windows/hooks/super-mode-consult-gate.js:264`／:387）。與 `E12` 訊息分流相關，單改拒絕訊息不能覆蓋免憑證放行路徑。 | `A0923-B17`；只登錄、不新增規則 |

### C. 使用者裁決（非 repo 變更）

| 項目 | 說明 | 來源 |
|---|---|---|
| <a id="LOCAL-EXECPOLICY-NARROW"></a>**✅ 使用者已裁決並於 2026-09-23 套用（非 repo 變更）**（ID：`LOCAL-EXECPOLICY-NARROW`） | 本機 `~/.codex/rules/default.rules` 已先備份再收窄：刪除 9 條 allow（`python -m pytest`、`soffice --headless`、`conda run -n mineru mineru`、`mineru-models-download`、`qlmanage -t`、`git ls-remote`、`gh auth status`、`ps -Ao`、一支 skill-installer 查詢腳本）；5 條改成 prompt（`git add`、`git restore --staged`、`git worktree add`、`git fetch origin`、`git fetch <另一個 remote>`）；保留 3 條 allow（`git worktree list`、`codex auth status`、`codex --help`）。依使用者回報，已用 `codex execpolicy check` 核對，並用同樣的 live 實驗覆驗：pytest 回到沙箱內執行（read-only 下因沒有可寫暫存目錄而失敗，conftest 沒有寫到外部）；`git add` 被拒（`approval required, AskForApproval=Never`），index 沒變。**這只收窄了這台機器這一份 rules 檔；產品缺陷 [`EXECPOLICY-ALLOW-INHERIT`](#EXECPOLICY-ALLOW-INHERIT) 照舊成立，其他使用者／其他 rules 層不受影響**，與產品解凍分開。刪除 allow 與改 prompt 的語義不同，後者在 approval=never 下拒絕可能正是預期控制。不能只依常見用法排序，也不能只改一個檔便推論有效 rules 已無同等 allow。 | `C0923` A1–A5＋使用者最終裁決與套用／覆驗回報（2026-09-23） |
| <a id="BASELINE-ACCEPT-0923"></a>**⬜ 使用者裁決：baseline 接受與否**（ID：`BASELINE-ACCEPT-0923`） | 由使用者決定，不在 repo 內處理；先看桌面控制／帳號工具的有效權限，再看 superpowers／finlab 的來源、解析 commit 與更新方式，最後才看無關 app。2026-09-23 macOS 未執行 `--update-baseline`，舊 baseline（0.144.5，2026-07-17）保留；新增 7 個 openai-curated-remote 第三方 app、多個帳號 connector 改由 curated-remote 提供、superpowers 改由 git 來源（obra/superpowers，ref 是 tag）；列為「此次 CLI 盤點未列出」的項目在 config 內仍 enabled，不能當成能力已移除。baseline 是接受紀錄，不是權限控制；hooks 有效集合仍未知，見 [`CODEX-CHECK-HOOKS-INVENTORY`](#CODEX-CHECK-HOOKS-INVENTORY)。 | `E0923`＋`C0923` B1–B4＋最終裁決 |

## 已完成（留紀錄，避免重複開題）

| 項目 | 處置 |
|---|---|
| ~~repo 缺 `.gitattributes`~~ | **2026-07-27 已加**。`* text=auto` 打底；`*.sh` 一律 `eol=lf`，另**逐檔明列** `codex-check-stubs/codex` 與 `codex-check-stubs/npm`（`linux/` 與 `macos/` 各 2 個、共 4 個無副檔名的 `#!/bin/bash` 腳本，`windows/` 沒有該目錄；`*.sh` 抓不到它們。刻意不用萬用字元，否則日後放進該目錄的 binary fixture 會被強制當文字正規化）；`*.js`／`*.json`／`*.yml`／`*.md` 也是 `eol=lf`；`*.ps1` 為 `eol=crlf`（Windows 原生執行，另有 UTF-8 BOM 需求——BOM 與行尾是兩回事）。加入後 `git status` 無偽差異，`git ls-files --eol` 確認 index 全部 `i/lf`。⚠️ **只保證新的 checkout**：既有 clone 的工作目錄不會因為加了本檔就自動重寫（詳見檔頭註解）|
| ~~`AI-INSTALL.md` 備份步驟會製造重複 skill~~ | **2026-07-27 已修**。備份改放 `~/.claude/skills-backup/`——原本放在 `~/.claude/skills/` 底下會被 skill loader 註冊成第二個 skill（description 幾乎相同、干擾選擇）。rollback 路徑一併更新，且還原改用**複製**而非搬移，備份因此留在原地、回滾可重複執行。殘留檔（`FIX-PLAN.md`）改用 1c 的逐一刪除處理，**安裝流程維持「從不整個刪除 live」** |
